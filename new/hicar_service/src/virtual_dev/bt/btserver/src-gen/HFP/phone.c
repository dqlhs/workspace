/*
 * Generated by gdbus-codegen 2.40.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "phone.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsaeyz.bluetooth.hfp
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Hfp
 * @title: Hfp
 * @short_description: Generated C code for the com.hsaeyz.bluetooth.hfp D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsaeyz.bluetooth.hfp ---- */

static const _ExtendedGDBusMethodInfo _hfp_method_info_answer_call =
{
  {
    -1,
    (gchar *) "answerCall",
    NULL,
    NULL,
    NULL
  },
  "handle-answer-call",
  FALSE
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_reject_call =
{
  {
    -1,
    (gchar *) "rejectCall",
    NULL,
    NULL,
    NULL
  },
  "handle-reject-call",
  FALSE
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_terminate_call =
{
  {
    -1,
    (gchar *) "terminateCall",
    NULL,
    NULL,
    NULL
  },
  "handle-terminate-call",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_hold_call_IN_ARG_action =
{
  {
    -1,
    (gchar *) "action",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_hold_call_IN_ARG_pointers[] =
{
  &_hfp_method_info_hold_call_IN_ARG_action,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_hold_call =
{
  {
    -1,
    (gchar *) "holdCall",
    (GDBusArgInfo **) &_hfp_method_info_hold_call_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-hold-call",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_send_dtmf_IN_ARG_dtmf =
{
  {
    -1,
    (gchar *) "dtmf",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_send_dtmf_IN_ARG_pointers[] =
{
  &_hfp_method_info_send_dtmf_IN_ARG_dtmf,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_send_dtmf =
{
  {
    -1,
    (gchar *) "sendDTMF",
    (GDBusArgInfo **) &_hfp_method_info_send_dtmf_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-send-dtmf",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_dail_call_IN_ARG_number =
{
  {
    -1,
    (gchar *) "number",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_dail_call_IN_ARG_pointers[] =
{
  &_hfp_method_info_dail_call_IN_ARG_number,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_dail_call =
{
  {
    -1,
    (gchar *) "dailCall",
    (GDBusArgInfo **) &_hfp_method_info_dail_call_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-dail-call",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_audio_transfer_IN_ARG_iActive =
{
  {
    -1,
    (gchar *) "iActive",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_audio_transfer_IN_ARG_pointers[] =
{
  &_hfp_method_info_audio_transfer_IN_ARG_iActive,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_audio_transfer =
{
  {
    -1,
    (gchar *) "audioTransfer",
    (GDBusArgInfo **) &_hfp_method_info_audio_transfer_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-audio-transfer",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_mute_mic_IN_ARG_micstate =
{
  {
    -1,
    (gchar *) "micstate",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_mute_mic_IN_ARG_pointers[] =
{
  &_hfp_method_info_mute_mic_IN_ARG_micstate,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_mute_mic =
{
  {
    -1,
    (gchar *) "MuteMic",
    (GDBusArgInfo **) &_hfp_method_info_mute_mic_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-mute-mic",
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_method_info_set_call_auto_action_IN_ARG_action =
{
  {
    -1,
    (gchar *) "action",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_method_info_set_call_auto_action_IN_ARG_pointers[] =
{
  &_hfp_method_info_set_call_auto_action_IN_ARG_action,
  NULL
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_set_call_auto_action =
{
  {
    -1,
    (gchar *) "setCallAutoAction",
    (GDBusArgInfo **) &_hfp_method_info_set_call_auto_action_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-call-auto-action",
  FALSE
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_play_ring_tone =
{
  {
    -1,
    (gchar *) "playRingTone",
    NULL,
    NULL,
    NULL
  },
  "handle-play-ring-tone",
  FALSE
};

static const _ExtendedGDBusMethodInfo _hfp_method_info_stop_ring_tone =
{
  {
    -1,
    (gchar *) "stopRingTone",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-ring-tone",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _hfp_method_info_pointers[] =
{
  &_hfp_method_info_answer_call,
  &_hfp_method_info_reject_call,
  &_hfp_method_info_terminate_call,
  &_hfp_method_info_hold_call,
  &_hfp_method_info_send_dtmf,
  &_hfp_method_info_dail_call,
  &_hfp_method_info_audio_transfer,
  &_hfp_method_info_mute_mic,
  &_hfp_method_info_set_call_auto_action,
  &_hfp_method_info_play_ring_tone,
  &_hfp_method_info_stop_ring_tone,
  NULL
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_hsaeasr_state_changed_ARG_addr =
{
  {
    -1,
    (gchar *) "addr",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_hsaeasr_state_changed_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_signal_info_hsaeasr_state_changed_ARG_pointers[] =
{
  &_hfp_signal_info_hsaeasr_state_changed_ARG_addr,
  &_hfp_signal_info_hsaeasr_state_changed_ARG_state,
  NULL
};

static const _ExtendedGDBusSignalInfo _hfp_signal_info_hsaeasr_state_changed =
{
  {
    -1,
    (gchar *) "hsaeasrStateChanged",
    (GDBusArgInfo **) &_hfp_signal_info_hsaeasr_state_changed_ARG_pointers,
    NULL
  },
  "hsaeasr-state-changed"
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_call_number_changed_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_call_number_changed_ARG_phoneNumber =
{
  {
    -1,
    (gchar *) "phoneNumber",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_call_number_changed_ARG_index =
{
  {
    -1,
    (gchar *) "index",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_signal_info_call_number_changed_ARG_pointers[] =
{
  &_hfp_signal_info_call_number_changed_ARG_name,
  &_hfp_signal_info_call_number_changed_ARG_phoneNumber,
  &_hfp_signal_info_call_number_changed_ARG_index,
  NULL
};

static const _ExtendedGDBusSignalInfo _hfp_signal_info_call_number_changed =
{
  {
    -1,
    (gchar *) "CallNumberChanged",
    (GDBusArgInfo **) &_hfp_signal_info_call_number_changed_ARG_pointers,
    NULL
  },
  "call-number-changed"
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_call_state_changed_ARG_callState =
{
  {
    -1,
    (gchar *) "callState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_call_state_changed_ARG_index =
{
  {
    -1,
    (gchar *) "index",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_signal_info_call_state_changed_ARG_pointers[] =
{
  &_hfp_signal_info_call_state_changed_ARG_callState,
  &_hfp_signal_info_call_state_changed_ARG_index,
  NULL
};

static const _ExtendedGDBusSignalInfo _hfp_signal_info_call_state_changed =
{
  {
    -1,
    (gchar *) "CallStateChanged",
    (GDBusArgInfo **) &_hfp_signal_info_call_state_changed_ARG_pointers,
    NULL
  },
  "call-state-changed"
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_private_state_changed_ARG_privateState =
{
  {
    -1,
    (gchar *) "privateState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_signal_info_private_state_changed_ARG_pointers[] =
{
  &_hfp_signal_info_private_state_changed_ARG_privateState,
  NULL
};

static const _ExtendedGDBusSignalInfo _hfp_signal_info_private_state_changed =
{
  {
    -1,
    (gchar *) "PrivateStateChanged",
    (GDBusArgInfo **) &_hfp_signal_info_private_state_changed_ARG_pointers,
    NULL
  },
  "private-state-changed"
};

static const _ExtendedGDBusArgInfo _hfp_signal_info_havering_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _hfp_signal_info_havering_ARG_pointers[] =
{
  &_hfp_signal_info_havering_ARG_state,
  NULL
};

static const _ExtendedGDBusSignalInfo _hfp_signal_info_havering =
{
  {
    -1,
    (gchar *) "Havering",
    (GDBusArgInfo **) &_hfp_signal_info_havering_ARG_pointers,
    NULL
  },
  "havering"
};

static const _ExtendedGDBusSignalInfo * const _hfp_signal_info_pointers[] =
{
  &_hfp_signal_info_hsaeasr_state_changed,
  &_hfp_signal_info_call_number_changed,
  &_hfp_signal_info_call_state_changed,
  &_hfp_signal_info_private_state_changed,
  &_hfp_signal_info_havering,
  NULL
};

static const _ExtendedGDBusPropertyInfo _hfp_property_info_private_state =
{
  {
    -1,
    (gchar *) "PrivateState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "private-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _hfp_property_info_hsaeasr_state =
{
  {
    -1,
    (gchar *) "hsaeasrState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "hsaeasr-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _hfp_property_info_pointers[] =
{
  &_hfp_property_info_private_state,
  &_hfp_property_info_hsaeasr_state,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _hfp_interface_info =
{
  {
    -1,
    (gchar *) "com.hsaeyz.bluetooth.hfp",
    (GDBusMethodInfo **) &_hfp_method_info_pointers,
    (GDBusSignalInfo **) &_hfp_signal_info_pointers,
    (GDBusPropertyInfo **) &_hfp_property_info_pointers,
    NULL
  },
  "hfp",
};


/**
 * hfp_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
hfp_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct;
}

/**
 * hfp_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Hfp interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
hfp_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "private-state");
  g_object_class_override_property (klass, property_id_begin++, "hsaeasr-state");
  return property_id_begin - 1;
}



/**
 * Hfp:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link>.
 */

/**
 * HfpIface:
 * @parent_iface: The parent interface.
 * @handle_answer_call: Handler for the #Hfp::handle-answer-call signal.
 * @handle_audio_transfer: Handler for the #Hfp::handle-audio-transfer signal.
 * @handle_dail_call: Handler for the #Hfp::handle-dail-call signal.
 * @handle_hold_call: Handler for the #Hfp::handle-hold-call signal.
 * @handle_mute_mic: Handler for the #Hfp::handle-mute-mic signal.
 * @handle_play_ring_tone: Handler for the #Hfp::handle-play-ring-tone signal.
 * @handle_reject_call: Handler for the #Hfp::handle-reject-call signal.
 * @handle_send_dtmf: Handler for the #Hfp::handle-send-dtmf signal.
 * @handle_set_call_auto_action: Handler for the #Hfp::handle-set-call-auto-action signal.
 * @handle_stop_ring_tone: Handler for the #Hfp::handle-stop-ring-tone signal.
 * @handle_terminate_call: Handler for the #Hfp::handle-terminate-call signal.
 * @get_hsaeasr_state: Getter for the #Hfp:hsaeasr-state property.
 * @get_private_state: Getter for the #Hfp:private-state property.
 * @call_number_changed: Handler for the #Hfp::call-number-changed signal.
 * @call_state_changed: Handler for the #Hfp::call-state-changed signal.
 * @havering: Handler for the #Hfp::havering signal.
 * @hsaeasr_state_changed: Handler for the #Hfp::hsaeasr-state-changed signal.
 * @private_state_changed: Handler for the #Hfp::private-state-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link>.
 */

typedef HfpIface HfpInterface;
G_DEFINE_INTERFACE (Hfp, hfp, G_TYPE_OBJECT);

static void
hfp_default_init (HfpIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Hfp::handle-answer-call:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.answerCall">answerCall()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_answer_call() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-answer-call",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_answer_call),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Hfp::handle-reject-call:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.rejectCall">rejectCall()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_reject_call() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-reject-call",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_reject_call),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Hfp::handle-terminate-call:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.terminateCall">terminateCall()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_terminate_call() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-terminate-call",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_terminate_call),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Hfp::handle-hold-call:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_action: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.holdCall">holdCall()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_hold_call() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-hold-call",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_hold_call),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * Hfp::handle-send-dtmf:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_dtmf: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.sendDTMF">sendDTMF()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_send_dtmf() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-send-dtmf",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_send_dtmf),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * Hfp::handle-dail-call:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_number: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.dailCall">dailCall()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_dail_call() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dail-call",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_dail_call),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * Hfp::handle-audio-transfer:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_iActive: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.audioTransfer">audioTransfer()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_audio_transfer() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-audio-transfer",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_audio_transfer),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Hfp::handle-mute-mic:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_micstate: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.MuteMic">MuteMic()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_mute_mic() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-mute-mic",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_mute_mic),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Hfp::handle-set-call-auto-action:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_action: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.setCallAutoAction">setCallAutoAction()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_set_call_auto_action() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-call-auto-action",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_set_call_auto_action),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * Hfp::handle-play-ring-tone:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.playRingTone">playRingTone()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_play_ring_tone() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play-ring-tone",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_play_ring_tone),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Hfp::handle-stop-ring-tone:
   * @object: A #Hfp.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.stopRingTone">stopRingTone()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call hfp_complete_stop_ring_tone() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-ring-tone",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, handle_stop_ring_tone),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * Hfp::hsaeasr-state-changed:
   * @object: A #Hfp.
   * @arg_addr: Argument.
   * @arg_state: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.hsaeasrStateChanged">"hsaeasrStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("hsaeasr-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, hsaeasr_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /**
   * Hfp::call-number-changed:
   * @object: A #Hfp.
   * @arg_name: Argument.
   * @arg_phoneNumber: Argument.
   * @arg_index: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.CallNumberChanged">"CallNumberChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("call-number-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, call_number_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Hfp::call-state-changed:
   * @object: A #Hfp.
   * @arg_callState: Argument.
   * @arg_index: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.CallStateChanged">"CallStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("call-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, call_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_INT);

  /**
   * Hfp::private-state-changed:
   * @object: A #Hfp.
   * @arg_privateState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.PrivateStateChanged">"PrivateStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("private-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, private_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /**
   * Hfp::havering:
   * @object: A #Hfp.
   * @arg_state: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.Havering">"Havering"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("havering",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (HfpIface, havering),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /* GObject properties for D-Bus properties: */
  /**
   * Hfp:private-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.PrivateState">"PrivateState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("private-state", "PrivateState", "PrivateState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Hfp:hsaeasr-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.hsaeasrState">"hsaeasrState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("hsaeasr-state", "hsaeasrState", "hsaeasrState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * hfp_get_private_state: (skip)
 * @object: A #Hfp.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.PrivateState">"PrivateState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
hfp_get_private_state (Hfp *object)
{
  return HFP_GET_IFACE (object)->get_private_state (object);
}

/**
 * hfp_set_private_state: (skip)
 * @object: A #Hfp.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.PrivateState">"PrivateState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
hfp_set_private_state (Hfp *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "private-state", value, NULL);
}

/**
 * hfp_get_hsaeasr_state: (skip)
 * @object: A #Hfp.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.hsaeasrState">"hsaeasrState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
hfp_get_hsaeasr_state (Hfp *object)
{
  return HFP_GET_IFACE (object)->get_hsaeasr_state (object);
}

/**
 * hfp_set_hsaeasr_state: (skip)
 * @object: A #Hfp.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-hfp.hsaeasrState">"hsaeasrState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
hfp_set_hsaeasr_state (Hfp *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "hsaeasr-state", value, NULL);
}

/**
 * hfp_emit_hsaeasr_state_changed:
 * @object: A #Hfp.
 * @arg_addr: Argument to pass with the signal.
 * @arg_state: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.hsaeasrStateChanged">"hsaeasrStateChanged"</link> D-Bus signal.
 */
void
hfp_emit_hsaeasr_state_changed (
    Hfp *object,
    const gchar *arg_addr,
    gboolean arg_state)
{
  g_signal_emit_by_name (object, "hsaeasr-state-changed", arg_addr, arg_state);
}

/**
 * hfp_emit_call_number_changed:
 * @object: A #Hfp.
 * @arg_name: Argument to pass with the signal.
 * @arg_phoneNumber: Argument to pass with the signal.
 * @arg_index: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.CallNumberChanged">"CallNumberChanged"</link> D-Bus signal.
 */
void
hfp_emit_call_number_changed (
    Hfp *object,
    const gchar *arg_name,
    const gchar *arg_phoneNumber,
    gint arg_index)
{
  g_signal_emit_by_name (object, "call-number-changed", arg_name, arg_phoneNumber, arg_index);
}

/**
 * hfp_emit_call_state_changed:
 * @object: A #Hfp.
 * @arg_callState: Argument to pass with the signal.
 * @arg_index: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.CallStateChanged">"CallStateChanged"</link> D-Bus signal.
 */
void
hfp_emit_call_state_changed (
    Hfp *object,
    gint arg_callState,
    gint arg_index)
{
  g_signal_emit_by_name (object, "call-state-changed", arg_callState, arg_index);
}

/**
 * hfp_emit_private_state_changed:
 * @object: A #Hfp.
 * @arg_privateState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.PrivateStateChanged">"PrivateStateChanged"</link> D-Bus signal.
 */
void
hfp_emit_private_state_changed (
    Hfp *object,
    gboolean arg_privateState)
{
  g_signal_emit_by_name (object, "private-state-changed", arg_privateState);
}

/**
 * hfp_emit_havering:
 * @object: A #Hfp.
 * @arg_state: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-hfp.Havering">"Havering"</link> D-Bus signal.
 */
void
hfp_emit_havering (
    Hfp *object,
    gboolean arg_state)
{
  g_signal_emit_by_name (object, "havering", arg_state);
}

/**
 * hfp_call_answer_call:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.answerCall">answerCall()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_answer_call_finish() to get the result of the operation.
 *
 * See hfp_call_answer_call_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_answer_call (
    Hfp *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "answerCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_answer_call_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_answer_call().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_answer_call().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_answer_call_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_answer_call_sync:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.answerCall">answerCall()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_answer_call() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_answer_call_sync (
    Hfp *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "answerCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_reject_call:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.rejectCall">rejectCall()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_reject_call_finish() to get the result of the operation.
 *
 * See hfp_call_reject_call_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_reject_call (
    Hfp *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "rejectCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_reject_call_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_reject_call().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_reject_call().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_reject_call_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_reject_call_sync:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.rejectCall">rejectCall()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_reject_call() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_reject_call_sync (
    Hfp *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "rejectCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_terminate_call:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.terminateCall">terminateCall()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_terminate_call_finish() to get the result of the operation.
 *
 * See hfp_call_terminate_call_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_terminate_call (
    Hfp *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "terminateCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_terminate_call_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_terminate_call().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_terminate_call().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_terminate_call_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_terminate_call_sync:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.terminateCall">terminateCall()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_terminate_call() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_terminate_call_sync (
    Hfp *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "terminateCall",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_hold_call:
 * @proxy: A #HfpProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.holdCall">holdCall()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_hold_call_finish() to get the result of the operation.
 *
 * See hfp_call_hold_call_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_hold_call (
    Hfp *proxy,
    gint arg_action,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "holdCall",
    g_variant_new ("(i)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_hold_call_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_hold_call().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_hold_call().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_hold_call_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_hold_call_sync:
 * @proxy: A #HfpProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.holdCall">holdCall()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_hold_call() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_hold_call_sync (
    Hfp *proxy,
    gint arg_action,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "holdCall",
    g_variant_new ("(i)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_send_dtmf:
 * @proxy: A #HfpProxy.
 * @arg_dtmf: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.sendDTMF">sendDTMF()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_send_dtmf_finish() to get the result of the operation.
 *
 * See hfp_call_send_dtmf_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_send_dtmf (
    Hfp *proxy,
    const gchar *arg_dtmf,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "sendDTMF",
    g_variant_new ("(s)",
                   arg_dtmf),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_send_dtmf_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_send_dtmf().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_send_dtmf().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_send_dtmf_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_send_dtmf_sync:
 * @proxy: A #HfpProxy.
 * @arg_dtmf: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.sendDTMF">sendDTMF()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_send_dtmf() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_send_dtmf_sync (
    Hfp *proxy,
    const gchar *arg_dtmf,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "sendDTMF",
    g_variant_new ("(s)",
                   arg_dtmf),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_dail_call:
 * @proxy: A #HfpProxy.
 * @arg_number: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.dailCall">dailCall()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_dail_call_finish() to get the result of the operation.
 *
 * See hfp_call_dail_call_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_dail_call (
    Hfp *proxy,
    const gchar *arg_number,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "dailCall",
    g_variant_new ("(s)",
                   arg_number),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_dail_call_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_dail_call().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_dail_call().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_dail_call_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_dail_call_sync:
 * @proxy: A #HfpProxy.
 * @arg_number: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.dailCall">dailCall()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_dail_call() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_dail_call_sync (
    Hfp *proxy,
    const gchar *arg_number,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "dailCall",
    g_variant_new ("(s)",
                   arg_number),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_audio_transfer:
 * @proxy: A #HfpProxy.
 * @arg_iActive: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.audioTransfer">audioTransfer()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_audio_transfer_finish() to get the result of the operation.
 *
 * See hfp_call_audio_transfer_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_audio_transfer (
    Hfp *proxy,
    gboolean arg_iActive,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "audioTransfer",
    g_variant_new ("(b)",
                   arg_iActive),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_audio_transfer_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_audio_transfer().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_audio_transfer().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_audio_transfer_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_audio_transfer_sync:
 * @proxy: A #HfpProxy.
 * @arg_iActive: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.audioTransfer">audioTransfer()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_audio_transfer() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_audio_transfer_sync (
    Hfp *proxy,
    gboolean arg_iActive,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "audioTransfer",
    g_variant_new ("(b)",
                   arg_iActive),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_mute_mic:
 * @proxy: A #HfpProxy.
 * @arg_micstate: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.MuteMic">MuteMic()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_mute_mic_finish() to get the result of the operation.
 *
 * See hfp_call_mute_mic_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_mute_mic (
    Hfp *proxy,
    gboolean arg_micstate,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "MuteMic",
    g_variant_new ("(b)",
                   arg_micstate),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_mute_mic_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_mute_mic().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_mute_mic().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_mute_mic_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_mute_mic_sync:
 * @proxy: A #HfpProxy.
 * @arg_micstate: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.MuteMic">MuteMic()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_mute_mic() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_mute_mic_sync (
    Hfp *proxy,
    gboolean arg_micstate,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "MuteMic",
    g_variant_new ("(b)",
                   arg_micstate),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_set_call_auto_action:
 * @proxy: A #HfpProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.setCallAutoAction">setCallAutoAction()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_set_call_auto_action_finish() to get the result of the operation.
 *
 * See hfp_call_set_call_auto_action_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_set_call_auto_action (
    Hfp *proxy,
    gint arg_action,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setCallAutoAction",
    g_variant_new ("(i)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_set_call_auto_action_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_set_call_auto_action().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_set_call_auto_action().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_set_call_auto_action_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_set_call_auto_action_sync:
 * @proxy: A #HfpProxy.
 * @arg_action: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.setCallAutoAction">setCallAutoAction()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_set_call_auto_action() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_set_call_auto_action_sync (
    Hfp *proxy,
    gint arg_action,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setCallAutoAction",
    g_variant_new ("(i)",
                   arg_action),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_play_ring_tone:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.playRingTone">playRingTone()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_play_ring_tone_finish() to get the result of the operation.
 *
 * See hfp_call_play_ring_tone_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_play_ring_tone (
    Hfp *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "playRingTone",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_play_ring_tone_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_play_ring_tone().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_play_ring_tone().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_play_ring_tone_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_play_ring_tone_sync:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.playRingTone">playRingTone()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_play_ring_tone() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_play_ring_tone_sync (
    Hfp *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "playRingTone",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_stop_ring_tone:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.stopRingTone">stopRingTone()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_call_stop_ring_tone_finish() to get the result of the operation.
 *
 * See hfp_call_stop_ring_tone_sync() for the synchronous, blocking version of this method.
 */
void
hfp_call_stop_ring_tone (
    Hfp *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stopRingTone",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * hfp_call_stop_ring_tone_finish:
 * @proxy: A #HfpProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_call_stop_ring_tone().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with hfp_call_stop_ring_tone().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_stop_ring_tone_finish (
    Hfp *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_call_stop_ring_tone_sync:
 * @proxy: A #HfpProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.stopRingTone">stopRingTone()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See hfp_call_stop_ring_tone() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
hfp_call_stop_ring_tone_sync (
    Hfp *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stopRingTone",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * hfp_complete_answer_call:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.answerCall">answerCall()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_answer_call (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_reject_call:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.rejectCall">rejectCall()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_reject_call (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_terminate_call:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.terminateCall">terminateCall()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_terminate_call (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_hold_call:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.holdCall">holdCall()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_hold_call (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_send_dtmf:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.sendDTMF">sendDTMF()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_send_dtmf (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_dail_call:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.dailCall">dailCall()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_dail_call (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_audio_transfer:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.audioTransfer">audioTransfer()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_audio_transfer (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_mute_mic:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.MuteMic">MuteMic()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_mute_mic (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_set_call_auto_action:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.setCallAutoAction">setCallAutoAction()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_set_call_auto_action (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_play_ring_tone:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.playRingTone">playRingTone()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_play_ring_tone (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * hfp_complete_stop_ring_tone:
 * @object: A #Hfp.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-hfp.stopRingTone">stopRingTone()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
hfp_complete_stop_ring_tone (
    Hfp *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * HfpProxy:
 *
 * The #HfpProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * HfpProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #HfpProxy.
 */

struct _HfpProxyPrivate
{
  GData *qdata;
};

static void hfp_proxy_iface_init (HfpIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (HfpProxy, hfp_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (HfpProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_HFP, hfp_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (HfpProxy, hfp_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_HFP, hfp_proxy_iface_init));

#endif
static void
hfp_proxy_finalize (GObject *object)
{
  HfpProxy *proxy = HFP_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (hfp_proxy_parent_class)->finalize (object);
}

static void
hfp_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _hfp_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
hfp_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.hsaeyz.bluetooth.hfp: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
hfp_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _hfp_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.hsaeyz.bluetooth.hfp", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) hfp_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
hfp_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_HFP);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_HFP);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
hfp_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  HfpProxy *proxy = HFP_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
hfp_proxy_get_private_state (Hfp *object)
{
  HfpProxy *proxy = HFP_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PrivateState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
hfp_proxy_get_hsaeasr_state (Hfp *object)
{
  HfpProxy *proxy = HFP_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "hsaeasrState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
hfp_proxy_init (HfpProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = hfp_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_HFP_PROXY, HfpProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), hfp_interface_info ());
}

static void
hfp_proxy_class_init (HfpProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = hfp_proxy_finalize;
  gobject_class->get_property = hfp_proxy_get_property;
  gobject_class->set_property = hfp_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = hfp_proxy_g_signal;
  proxy_class->g_properties_changed = hfp_proxy_g_properties_changed;

  hfp_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (HfpProxyPrivate));
#endif
}

static void
hfp_proxy_iface_init (HfpIface *iface)
{
  iface->get_private_state = hfp_proxy_get_private_state;
  iface->get_hsaeasr_state = hfp_proxy_get_hsaeasr_state;
}

/**
 * hfp_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_proxy_new_finish() to get the result of the operation.
 *
 * See hfp_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
hfp_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_HFP_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.hfp", NULL);
}

/**
 * hfp_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with hfp_proxy_new().
 *
 * Returns: (transfer full) (type HfpProxy): The constructed proxy object or %NULL if @error is set.
 */
Hfp *
hfp_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return HFP (ret);
  else
    return NULL;
}

/**
 * hfp_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See hfp_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type HfpProxy): The constructed proxy object or %NULL if @error is set.
 */
Hfp *
hfp_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_HFP_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.hfp", NULL);
  if (ret != NULL)
    return HFP (ret);
  else
    return NULL;
}


/**
 * hfp_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like hfp_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call hfp_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See hfp_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
hfp_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_HFP_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.hfp", NULL);
}

/**
 * hfp_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to hfp_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with hfp_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type HfpProxy): The constructed proxy object or %NULL if @error is set.
 */
Hfp *
hfp_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return HFP (ret);
  else
    return NULL;
}

/**
 * hfp_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like hfp_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See hfp_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type HfpProxy): The constructed proxy object or %NULL if @error is set.
 */
Hfp *
hfp_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_HFP_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.hfp", NULL);
  if (ret != NULL)
    return HFP (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * HfpSkeleton:
 *
 * The #HfpSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * HfpSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #HfpSkeleton.
 */

struct _HfpSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_hfp_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  HfpSkeleton *skeleton = HFP_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_HFP);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_HFP);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_hfp_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  HfpSkeleton *skeleton = HFP_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_hfp_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  HfpSkeleton *skeleton = HFP_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_hfp_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _hfp_skeleton_vtable =
{
  _hfp_skeleton_handle_method_call,
  _hfp_skeleton_handle_get_property,
  _hfp_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
hfp_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return hfp_interface_info ();
}

static GDBusInterfaceVTable *
hfp_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_hfp_skeleton_vtable;
}

static GVariant *
hfp_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  HfpSkeleton *skeleton = HFP_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_hfp_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _hfp_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _hfp_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _hfp_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _hfp_emit_changed (gpointer user_data);

static void
hfp_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  HfpSkeleton *skeleton = HFP_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _hfp_emit_changed (skeleton);
}

static void
_hfp_on_signal_hsaeasr_state_changed (
    Hfp *object,
    const gchar *arg_addr,
    gboolean arg_state)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(sb)",
                   arg_addr,
                   arg_state));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", "hsaeasrStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_hfp_on_signal_call_number_changed (
    Hfp *object,
    const gchar *arg_name,
    const gchar *arg_phoneNumber,
    gint arg_index)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ssi)",
                   arg_name,
                   arg_phoneNumber,
                   arg_index));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", "CallNumberChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_hfp_on_signal_call_state_changed (
    Hfp *object,
    gint arg_callState,
    gint arg_index)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ii)",
                   arg_callState,
                   arg_index));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", "CallStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_hfp_on_signal_private_state_changed (
    Hfp *object,
    gboolean arg_privateState)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_privateState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", "PrivateStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_hfp_on_signal_havering (
    Hfp *object,
    gboolean arg_state)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_state));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.hfp", "Havering",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void hfp_skeleton_iface_init (HfpIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (HfpSkeleton, hfp_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (HfpSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_HFP, hfp_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (HfpSkeleton, hfp_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_HFP, hfp_skeleton_iface_init));

#endif
static void
hfp_skeleton_finalize (GObject *object)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (hfp_skeleton_parent_class)->finalize (object);
}

static void
hfp_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_hfp_emit_changed (gpointer user_data)
{
  HfpSkeleton *skeleton = HFP_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.hsaeyz.bluetooth.hfp",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_hfp_schedule_emit_changed (HfpSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
hfp_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _hfp_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
hfp_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _hfp_schedule_emit_changed (skeleton, _hfp_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
hfp_skeleton_init (HfpSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = hfp_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_HFP_SKELETON, HfpSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
}

static gboolean 
hfp_skeleton_get_private_state (Hfp *object)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
hfp_skeleton_get_hsaeasr_state (Hfp *object)
{
  HfpSkeleton *skeleton = HFP_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
hfp_skeleton_class_init (HfpSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = hfp_skeleton_finalize;
  gobject_class->get_property = hfp_skeleton_get_property;
  gobject_class->set_property = hfp_skeleton_set_property;
  gobject_class->notify       = hfp_skeleton_notify;


  hfp_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = hfp_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = hfp_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = hfp_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = hfp_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (HfpSkeletonPrivate));
#endif
}

static void
hfp_skeleton_iface_init (HfpIface *iface)
{
  iface->hsaeasr_state_changed = _hfp_on_signal_hsaeasr_state_changed;
  iface->call_number_changed = _hfp_on_signal_call_number_changed;
  iface->call_state_changed = _hfp_on_signal_call_state_changed;
  iface->private_state_changed = _hfp_on_signal_private_state_changed;
  iface->havering = _hfp_on_signal_havering;
  iface->get_private_state = hfp_skeleton_get_private_state;
  iface->get_hsaeasr_state = hfp_skeleton_get_hsaeasr_state;
}

/**
 * hfp_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-hfp.top_of_page">com.hsaeyz.bluetooth.hfp</link>.
 *
 * Returns: (transfer full) (type HfpSkeleton): The skeleton object.
 */
Hfp *
hfp_skeleton_new (void)
{
  return HFP (g_object_new (TYPE_HFP_SKELETON, NULL));
}

