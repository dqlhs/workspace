/*
 * Generated by gdbus-codegen 2.40.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "media.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsaeyz.bluetooth.media
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Media
 * @title: Media
 * @short_description: Generated C code for the com.hsaeyz.bluetooth.media D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsaeyz.bluetooth.media ---- */

static const _ExtendedGDBusMethodInfo _media_method_info_next_song =
{
  {
    -1,
    (gchar *) "nextSong",
    NULL,
    NULL,
    NULL
  },
  "handle-next-song",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_pause_music =
{
  {
    -1,
    (gchar *) "pauseMusic",
    NULL,
    NULL,
    NULL
  },
  "handle-pause-music",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_play_music =
{
  {
    -1,
    (gchar *) "playMusic",
    NULL,
    NULL,
    NULL
  },
  "handle-play-music",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_last_song =
{
  {
    -1,
    (gchar *) "lastSong",
    NULL,
    NULL,
    NULL
  },
  "handle-last-song",
  FALSE
};

static const _ExtendedGDBusArgInfo _media_method_info_mute_music_IN_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_method_info_mute_music_IN_ARG_pointers[] =
{
  &_media_method_info_mute_music_IN_ARG_state,
  NULL
};

static const _ExtendedGDBusMethodInfo _media_method_info_mute_music =
{
  {
    -1,
    (gchar *) "muteMusic",
    (GDBusArgInfo **) &_media_method_info_mute_music_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-mute-music",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_fast_forwardstart =
{
  {
    -1,
    (gchar *) "fastForwardstart",
    NULL,
    NULL,
    NULL
  },
  "handle-fast-forwardstart",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_fast_forwardstop =
{
  {
    -1,
    (gchar *) "fastForwardstop",
    NULL,
    NULL,
    NULL
  },
  "handle-fast-forwardstop",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_rewindstart =
{
  {
    -1,
    (gchar *) "rewindstart",
    NULL,
    NULL,
    NULL
  },
  "handle-rewindstart",
  FALSE
};

static const _ExtendedGDBusMethodInfo _media_method_info_rewindstop =
{
  {
    -1,
    (gchar *) "rewindstop",
    NULL,
    NULL,
    NULL
  },
  "handle-rewindstop",
  FALSE
};

static const _ExtendedGDBusArgInfo _media_method_info_setplayer_IN_ARG_nAttrID =
{
  {
    -1,
    (gchar *) "nAttrID",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_method_info_setplayer_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_method_info_setplayer_IN_ARG_pointers[] =
{
  &_media_method_info_setplayer_IN_ARG_nAttrID,
  &_media_method_info_setplayer_IN_ARG_value,
  NULL
};

static const _ExtendedGDBusMethodInfo _media_method_info_setplayer =
{
  {
    -1,
    (gchar *) "setplayer",
    (GDBusArgInfo **) &_media_method_info_setplayer_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-setplayer",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _media_method_info_pointers[] =
{
  &_media_method_info_next_song,
  &_media_method_info_pause_music,
  &_media_method_info_play_music,
  &_media_method_info_last_song,
  &_media_method_info_mute_music,
  &_media_method_info_fast_forwardstart,
  &_media_method_info_fast_forwardstop,
  &_media_method_info_rewindstart,
  &_media_method_info_rewindstop,
  &_media_method_info_setplayer,
  NULL
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_attributes_changed_ARG_title =
{
  {
    -1,
    (gchar *) "title",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_attributes_changed_ARG_artist =
{
  {
    -1,
    (gchar *) "artist",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_attributes_changed_ARG_album =
{
  {
    -1,
    (gchar *) "album",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_attributes_changed_ARG_genre =
{
  {
    -1,
    (gchar *) "genre",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_attributes_changed_ARG_trackNumber =
{
  {
    -1,
    (gchar *) "trackNumber",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_signal_info_play_attributes_changed_ARG_pointers[] =
{
  &_media_signal_info_play_attributes_changed_ARG_title,
  &_media_signal_info_play_attributes_changed_ARG_artist,
  &_media_signal_info_play_attributes_changed_ARG_album,
  &_media_signal_info_play_attributes_changed_ARG_genre,
  &_media_signal_info_play_attributes_changed_ARG_trackNumber,
  NULL
};

static const _ExtendedGDBusSignalInfo _media_signal_info_play_attributes_changed =
{
  {
    -1,
    (gchar *) "PlayAttributesChanged",
    (GDBusArgInfo **) &_media_signal_info_play_attributes_changed_ARG_pointers,
    NULL
  },
  "play-attributes-changed"
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_status_changed_ARG_playState =
{
  {
    -1,
    (gchar *) "playState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_status_changed_ARG_songLength =
{
  {
    -1,
    (gchar *) "songLength",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_play_status_changed_ARG_playPosition =
{
  {
    -1,
    (gchar *) "playPosition",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_signal_info_play_status_changed_ARG_pointers[] =
{
  &_media_signal_info_play_status_changed_ARG_playState,
  &_media_signal_info_play_status_changed_ARG_songLength,
  &_media_signal_info_play_status_changed_ARG_playPosition,
  NULL
};

static const _ExtendedGDBusSignalInfo _media_signal_info_play_status_changed =
{
  {
    -1,
    (gchar *) "PlayStatusChanged",
    (GDBusArgInfo **) &_media_signal_info_play_status_changed_ARG_pointers,
    NULL
  },
  "play-status-changed"
};

static const _ExtendedGDBusArgInfo _media_signal_info_stream_status_changed_ARG_streamStatus =
{
  {
    -1,
    (gchar *) "streamStatus",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_signal_info_stream_status_changed_ARG_pointers[] =
{
  &_media_signal_info_stream_status_changed_ARG_streamStatus,
  NULL
};

static const _ExtendedGDBusSignalInfo _media_signal_info_stream_status_changed =
{
  {
    -1,
    (gchar *) "streamStatusChanged",
    (GDBusArgInfo **) &_media_signal_info_stream_status_changed_ARG_pointers,
    NULL
  },
  "stream-status-changed"
};

static const _ExtendedGDBusArgInfo _media_signal_info_capability_changed_ARG_ncount =
{
  {
    -1,
    (gchar *) "ncount",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_capability_changed_ARG_eventname =
{
  {
    -1,
    (gchar *) "eventname",
    (gchar *) "ay",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_signal_info_capability_changed_ARG_pointers[] =
{
  &_media_signal_info_capability_changed_ARG_ncount,
  &_media_signal_info_capability_changed_ARG_eventname,
  NULL
};

static const _ExtendedGDBusSignalInfo _media_signal_info_capability_changed =
{
  {
    -1,
    (gchar *) "capabilityChanged",
    (GDBusArgInfo **) &_media_signal_info_capability_changed_ARG_pointers,
    NULL
  },
  "capability-changed"
};

static const _ExtendedGDBusArgInfo _media_signal_info_playermodes_changed_ARG_shuffle =
{
  {
    -1,
    (gchar *) "shuffle",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _media_signal_info_playermodes_changed_ARG_repeate =
{
  {
    -1,
    (gchar *) "repeate",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _media_signal_info_playermodes_changed_ARG_pointers[] =
{
  &_media_signal_info_playermodes_changed_ARG_shuffle,
  &_media_signal_info_playermodes_changed_ARG_repeate,
  NULL
};

static const _ExtendedGDBusSignalInfo _media_signal_info_playermodes_changed =
{
  {
    -1,
    (gchar *) "playermodesChanged",
    (GDBusArgInfo **) &_media_signal_info_playermodes_changed_ARG_pointers,
    NULL
  },
  "playermodes-changed"
};

static const _ExtendedGDBusSignalInfo * const _media_signal_info_pointers[] =
{
  &_media_signal_info_play_attributes_changed,
  &_media_signal_info_play_status_changed,
  &_media_signal_info_stream_status_changed,
  &_media_signal_info_capability_changed,
  &_media_signal_info_playermodes_changed,
  NULL
};

static const _ExtendedGDBusPropertyInfo _media_property_info_music_mute_state =
{
  {
    -1,
    (gchar *) "MusicMuteState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "music-mute-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _media_property_info_shufflemode =
{
  {
    -1,
    (gchar *) "Shufflemode",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "shufflemode",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _media_property_info_reaptemode =
{
  {
    -1,
    (gchar *) "Reaptemode",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "reaptemode",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _media_property_info_pointers[] =
{
  &_media_property_info_music_mute_state,
  &_media_property_info_shufflemode,
  &_media_property_info_reaptemode,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _media_interface_info =
{
  {
    -1,
    (gchar *) "com.hsaeyz.bluetooth.media",
    (GDBusMethodInfo **) &_media_method_info_pointers,
    (GDBusSignalInfo **) &_media_signal_info_pointers,
    (GDBusPropertyInfo **) &_media_property_info_pointers,
    NULL
  },
  "media",
};


/**
 * media_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
media_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_media_interface_info.parent_struct;
}

/**
 * media_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Media interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
media_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "music-mute-state");
  g_object_class_override_property (klass, property_id_begin++, "shufflemode");
  g_object_class_override_property (klass, property_id_begin++, "reaptemode");
  return property_id_begin - 1;
}



/**
 * Media:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link>.
 */

/**
 * MediaIface:
 * @parent_iface: The parent interface.
 * @handle_fast_forwardstart: Handler for the #Media::handle-fast-forwardstart signal.
 * @handle_fast_forwardstop: Handler for the #Media::handle-fast-forwardstop signal.
 * @handle_last_song: Handler for the #Media::handle-last-song signal.
 * @handle_mute_music: Handler for the #Media::handle-mute-music signal.
 * @handle_next_song: Handler for the #Media::handle-next-song signal.
 * @handle_pause_music: Handler for the #Media::handle-pause-music signal.
 * @handle_play_music: Handler for the #Media::handle-play-music signal.
 * @handle_rewindstart: Handler for the #Media::handle-rewindstart signal.
 * @handle_rewindstop: Handler for the #Media::handle-rewindstop signal.
 * @handle_setplayer: Handler for the #Media::handle-setplayer signal.
 * @get_music_mute_state: Getter for the #Media:music-mute-state property.
 * @get_reaptemode: Getter for the #Media:reaptemode property.
 * @get_shufflemode: Getter for the #Media:shufflemode property.
 * @capability_changed: Handler for the #Media::capability-changed signal.
 * @play_attributes_changed: Handler for the #Media::play-attributes-changed signal.
 * @play_status_changed: Handler for the #Media::play-status-changed signal.
 * @playermodes_changed: Handler for the #Media::playermodes-changed signal.
 * @stream_status_changed: Handler for the #Media::stream-status-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link>.
 */

typedef MediaIface MediaInterface;
G_DEFINE_INTERFACE (Media, media, G_TYPE_OBJECT);

static void
media_default_init (MediaIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Media::handle-next-song:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.nextSong">nextSong()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_next_song() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-next-song",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_next_song),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-pause-music:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.pauseMusic">pauseMusic()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_pause_music() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pause-music",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_pause_music),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-play-music:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.playMusic">playMusic()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_play_music() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play-music",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_play_music),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-last-song:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.lastSong">lastSong()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_last_song() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-last-song",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_last_song),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-mute-music:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_state: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.muteMusic">muteMusic()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_mute_music() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-mute-music",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_mute_music),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Media::handle-fast-forwardstart:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstart">fastForwardstart()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_fast_forwardstart() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fast-forwardstart",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_fast_forwardstart),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-fast-forwardstop:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstop">fastForwardstop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_fast_forwardstop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-fast-forwardstop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_fast_forwardstop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-rewindstart:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstart">rewindstart()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_rewindstart() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-rewindstart",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_rewindstart),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-rewindstop:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstop">rewindstop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_rewindstop() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-rewindstop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_rewindstop),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Media::handle-setplayer:
   * @object: A #Media.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_nAttrID: Argument passed by remote caller.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.setplayer">setplayer()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call media_complete_setplayer() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-setplayer",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, handle_setplayer),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT, G_TYPE_INT);

  /* GObject signals for received D-Bus signals: */
  /**
   * Media::play-attributes-changed:
   * @object: A #Media.
   * @arg_title: Argument.
   * @arg_artist: Argument.
   * @arg_album: Argument.
   * @arg_genre: Argument.
   * @arg_trackNumber: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.PlayAttributesChanged">"PlayAttributesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("play-attributes-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, play_attributes_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    5, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Media::play-status-changed:
   * @object: A #Media.
   * @arg_playState: Argument.
   * @arg_songLength: Argument.
   * @arg_playPosition: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.PlayStatusChanged">"PlayStatusChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("play-status-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, play_status_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_INT, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * Media::stream-status-changed:
   * @object: A #Media.
   * @arg_streamStatus: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.streamStatusChanged">"streamStatusChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("stream-status-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, stream_status_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /**
   * Media::capability-changed:
   * @object: A #Media.
   * @arg_ncount: Argument.
   * @arg_eventname: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.capabilityChanged">"capabilityChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("capability-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, capability_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_STRING);

  /**
   * Media::playermodes-changed:
   * @object: A #Media.
   * @arg_shuffle: Argument.
   * @arg_repeate: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.playermodesChanged">"playermodesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("playermodes-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (MediaIface, playermodes_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_INT);

  /* GObject properties for D-Bus properties: */
  /**
   * Media:music-mute-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.MusicMuteState">"MusicMuteState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("music-mute-state", "MusicMuteState", "MusicMuteState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Media:shufflemode:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Shufflemode">"Shufflemode"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("shufflemode", "Shufflemode", "Shufflemode", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Media:reaptemode:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Reaptemode">"Reaptemode"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("reaptemode", "Reaptemode", "Reaptemode", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * media_get_music_mute_state: (skip)
 * @object: A #Media.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.MusicMuteState">"MusicMuteState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
media_get_music_mute_state (Media *object)
{
  return MEDIA_GET_IFACE (object)->get_music_mute_state (object);
}

/**
 * media_set_music_mute_state: (skip)
 * @object: A #Media.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.MusicMuteState">"MusicMuteState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
media_set_music_mute_state (Media *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "music-mute-state", value, NULL);
}

/**
 * media_get_shufflemode: (skip)
 * @object: A #Media.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Shufflemode">"Shufflemode"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
media_get_shufflemode (Media *object)
{
  return MEDIA_GET_IFACE (object)->get_shufflemode (object);
}

/**
 * media_set_shufflemode: (skip)
 * @object: A #Media.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Shufflemode">"Shufflemode"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
media_set_shufflemode (Media *object, gint value)
{
  g_object_set (G_OBJECT (object), "shufflemode", value, NULL);
}

/**
 * media_get_reaptemode: (skip)
 * @object: A #Media.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Reaptemode">"Reaptemode"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
media_get_reaptemode (Media *object)
{
  return MEDIA_GET_IFACE (object)->get_reaptemode (object);
}

/**
 * media_set_reaptemode: (skip)
 * @object: A #Media.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-media.Reaptemode">"Reaptemode"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
media_set_reaptemode (Media *object, gint value)
{
  g_object_set (G_OBJECT (object), "reaptemode", value, NULL);
}

/**
 * media_emit_play_attributes_changed:
 * @object: A #Media.
 * @arg_title: Argument to pass with the signal.
 * @arg_artist: Argument to pass with the signal.
 * @arg_album: Argument to pass with the signal.
 * @arg_genre: Argument to pass with the signal.
 * @arg_trackNumber: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.PlayAttributesChanged">"PlayAttributesChanged"</link> D-Bus signal.
 */
void
media_emit_play_attributes_changed (
    Media *object,
    const gchar *arg_title,
    const gchar *arg_artist,
    const gchar *arg_album,
    const gchar *arg_genre,
    gint arg_trackNumber)
{
  g_signal_emit_by_name (object, "play-attributes-changed", arg_title, arg_artist, arg_album, arg_genre, arg_trackNumber);
}

/**
 * media_emit_play_status_changed:
 * @object: A #Media.
 * @arg_playState: Argument to pass with the signal.
 * @arg_songLength: Argument to pass with the signal.
 * @arg_playPosition: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.PlayStatusChanged">"PlayStatusChanged"</link> D-Bus signal.
 */
void
media_emit_play_status_changed (
    Media *object,
    gint arg_playState,
    const gchar *arg_songLength,
    const gchar *arg_playPosition)
{
  g_signal_emit_by_name (object, "play-status-changed", arg_playState, arg_songLength, arg_playPosition);
}

/**
 * media_emit_stream_status_changed:
 * @object: A #Media.
 * @arg_streamStatus: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.streamStatusChanged">"streamStatusChanged"</link> D-Bus signal.
 */
void
media_emit_stream_status_changed (
    Media *object,
    gboolean arg_streamStatus)
{
  g_signal_emit_by_name (object, "stream-status-changed", arg_streamStatus);
}

/**
 * media_emit_capability_changed:
 * @object: A #Media.
 * @arg_ncount: Argument to pass with the signal.
 * @arg_eventname: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.capabilityChanged">"capabilityChanged"</link> D-Bus signal.
 */
void
media_emit_capability_changed (
    Media *object,
    gint arg_ncount,
    const gchar *arg_eventname)
{
  g_signal_emit_by_name (object, "capability-changed", arg_ncount, arg_eventname);
}

/**
 * media_emit_playermodes_changed:
 * @object: A #Media.
 * @arg_shuffle: Argument to pass with the signal.
 * @arg_repeate: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-media.playermodesChanged">"playermodesChanged"</link> D-Bus signal.
 */
void
media_emit_playermodes_changed (
    Media *object,
    gint arg_shuffle,
    gint arg_repeate)
{
  g_signal_emit_by_name (object, "playermodes-changed", arg_shuffle, arg_repeate);
}

/**
 * media_call_next_song:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.nextSong">nextSong()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_next_song_finish() to get the result of the operation.
 *
 * See media_call_next_song_sync() for the synchronous, blocking version of this method.
 */
void
media_call_next_song (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "nextSong",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_next_song_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_next_song().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_next_song().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_next_song_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_next_song_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.nextSong">nextSong()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_next_song() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_next_song_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "nextSong",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_pause_music:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.pauseMusic">pauseMusic()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_pause_music_finish() to get the result of the operation.
 *
 * See media_call_pause_music_sync() for the synchronous, blocking version of this method.
 */
void
media_call_pause_music (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "pauseMusic",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_pause_music_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_pause_music().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_pause_music().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_pause_music_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_pause_music_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.pauseMusic">pauseMusic()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_pause_music() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_pause_music_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "pauseMusic",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_play_music:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.playMusic">playMusic()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_play_music_finish() to get the result of the operation.
 *
 * See media_call_play_music_sync() for the synchronous, blocking version of this method.
 */
void
media_call_play_music (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "playMusic",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_play_music_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_play_music().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_play_music().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_play_music_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_play_music_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.playMusic">playMusic()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_play_music() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_play_music_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "playMusic",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_last_song:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.lastSong">lastSong()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_last_song_finish() to get the result of the operation.
 *
 * See media_call_last_song_sync() for the synchronous, blocking version of this method.
 */
void
media_call_last_song (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "lastSong",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_last_song_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_last_song().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_last_song().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_last_song_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_last_song_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.lastSong">lastSong()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_last_song() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_last_song_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "lastSong",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_mute_music:
 * @proxy: A #MediaProxy.
 * @arg_state: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.muteMusic">muteMusic()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_mute_music_finish() to get the result of the operation.
 *
 * See media_call_mute_music_sync() for the synchronous, blocking version of this method.
 */
void
media_call_mute_music (
    Media *proxy,
    gboolean arg_state,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "muteMusic",
    g_variant_new ("(b)",
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_mute_music_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_mute_music().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_mute_music().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_mute_music_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_mute_music_sync:
 * @proxy: A #MediaProxy.
 * @arg_state: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.muteMusic">muteMusic()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_mute_music() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_mute_music_sync (
    Media *proxy,
    gboolean arg_state,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "muteMusic",
    g_variant_new ("(b)",
                   arg_state),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_fast_forwardstart:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstart">fastForwardstart()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_fast_forwardstart_finish() to get the result of the operation.
 *
 * See media_call_fast_forwardstart_sync() for the synchronous, blocking version of this method.
 */
void
media_call_fast_forwardstart (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fastForwardstart",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_fast_forwardstart_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_fast_forwardstart().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_fast_forwardstart().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_fast_forwardstart_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_fast_forwardstart_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstart">fastForwardstart()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_fast_forwardstart() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_fast_forwardstart_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fastForwardstart",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_fast_forwardstop:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstop">fastForwardstop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_fast_forwardstop_finish() to get the result of the operation.
 *
 * See media_call_fast_forwardstop_sync() for the synchronous, blocking version of this method.
 */
void
media_call_fast_forwardstop (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "fastForwardstop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_fast_forwardstop_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_fast_forwardstop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_fast_forwardstop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_fast_forwardstop_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_fast_forwardstop_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstop">fastForwardstop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_fast_forwardstop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_fast_forwardstop_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "fastForwardstop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_rewindstart:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstart">rewindstart()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_rewindstart_finish() to get the result of the operation.
 *
 * See media_call_rewindstart_sync() for the synchronous, blocking version of this method.
 */
void
media_call_rewindstart (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "rewindstart",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_rewindstart_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_rewindstart().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_rewindstart().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_rewindstart_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_rewindstart_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstart">rewindstart()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_rewindstart() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_rewindstart_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "rewindstart",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_rewindstop:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstop">rewindstop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_rewindstop_finish() to get the result of the operation.
 *
 * See media_call_rewindstop_sync() for the synchronous, blocking version of this method.
 */
void
media_call_rewindstop (
    Media *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "rewindstop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_rewindstop_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_rewindstop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_rewindstop().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_rewindstop_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_rewindstop_sync:
 * @proxy: A #MediaProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstop">rewindstop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_rewindstop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_rewindstop_sync (
    Media *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "rewindstop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_setplayer:
 * @proxy: A #MediaProxy.
 * @arg_nAttrID: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.setplayer">setplayer()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_call_setplayer_finish() to get the result of the operation.
 *
 * See media_call_setplayer_sync() for the synchronous, blocking version of this method.
 */
void
media_call_setplayer (
    Media *proxy,
    gint arg_nAttrID,
    gint arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setplayer",
    g_variant_new ("(ii)",
                   arg_nAttrID,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * media_call_setplayer_finish:
 * @proxy: A #MediaProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_call_setplayer().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with media_call_setplayer().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_setplayer_finish (
    Media *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_call_setplayer_sync:
 * @proxy: A #MediaProxy.
 * @arg_nAttrID: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.setplayer">setplayer()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See media_call_setplayer() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
media_call_setplayer_sync (
    Media *proxy,
    gint arg_nAttrID,
    gint arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setplayer",
    g_variant_new ("(ii)",
                   arg_nAttrID,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * media_complete_next_song:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.nextSong">nextSong()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_next_song (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_pause_music:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.pauseMusic">pauseMusic()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_pause_music (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_play_music:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.playMusic">playMusic()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_play_music (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_last_song:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.lastSong">lastSong()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_last_song (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_mute_music:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.muteMusic">muteMusic()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_mute_music (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_fast_forwardstart:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstart">fastForwardstart()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_fast_forwardstart (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_fast_forwardstop:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.fastForwardstop">fastForwardstop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_fast_forwardstop (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_rewindstart:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstart">rewindstart()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_rewindstart (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_rewindstop:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.rewindstop">rewindstop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_rewindstop (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * media_complete_setplayer:
 * @object: A #Media.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-media.setplayer">setplayer()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
media_complete_setplayer (
    Media *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * MediaProxy:
 *
 * The #MediaProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediaProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediaProxy.
 */

struct _MediaProxyPrivate
{
  GData *qdata;
};

static void media_proxy_iface_init (MediaIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediaProxy, media_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (MediaProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIA, media_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediaProxy, media_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIA, media_proxy_iface_init));

#endif
static void
media_proxy_finalize (GObject *object)
{
  MediaProxy *proxy = MEDIA_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (media_proxy_parent_class)->finalize (object);
}

static void
media_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _media_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
media_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.hsaeyz.bluetooth.media: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
media_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  info = _media_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.hsaeyz.bluetooth.media", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) media_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
media_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_media_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_MEDIA);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIA);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
media_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  MediaProxy *proxy = MEDIA_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_media_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_media_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
media_proxy_get_music_mute_state (Media *object)
{
  MediaProxy *proxy = MEDIA_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MusicMuteState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
media_proxy_get_shufflemode (Media *object)
{
  MediaProxy *proxy = MEDIA_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Shufflemode");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
media_proxy_get_reaptemode (Media *object)
{
  MediaProxy *proxy = MEDIA_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Reaptemode");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
media_proxy_init (MediaProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = media_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_MEDIA_PROXY, MediaProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), media_interface_info ());
}

static void
media_proxy_class_init (MediaProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = media_proxy_finalize;
  gobject_class->get_property = media_proxy_get_property;
  gobject_class->set_property = media_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = media_proxy_g_signal;
  proxy_class->g_properties_changed = media_proxy_g_properties_changed;

  media_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediaProxyPrivate));
#endif
}

static void
media_proxy_iface_init (MediaIface *iface)
{
  iface->get_music_mute_state = media_proxy_get_music_mute_state;
  iface->get_shufflemode = media_proxy_get_shufflemode;
  iface->get_reaptemode = media_proxy_get_reaptemode;
}

/**
 * media_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_proxy_new_finish() to get the result of the operation.
 *
 * See media_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
media_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIA_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.media", NULL);
}

/**
 * media_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with media_proxy_new().
 *
 * Returns: (transfer full) (type MediaProxy): The constructed proxy object or %NULL if @error is set.
 */
Media *
media_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIA (ret);
  else
    return NULL;
}

/**
 * media_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See media_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediaProxy): The constructed proxy object or %NULL if @error is set.
 */
Media *
media_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIA_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.media", NULL);
  if (ret != NULL)
    return MEDIA (ret);
  else
    return NULL;
}


/**
 * media_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like media_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call media_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See media_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
media_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_MEDIA_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.media", NULL);
}

/**
 * media_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to media_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with media_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type MediaProxy): The constructed proxy object or %NULL if @error is set.
 */
Media *
media_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return MEDIA (ret);
  else
    return NULL;
}

/**
 * media_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like media_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See media_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type MediaProxy): The constructed proxy object or %NULL if @error is set.
 */
Media *
media_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_MEDIA_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.media", NULL);
  if (ret != NULL)
    return MEDIA (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * MediaSkeleton:
 *
 * The #MediaSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * MediaSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #MediaSkeleton.
 */

struct _MediaSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_media_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_MEDIA);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_MEDIA);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_media_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_media_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_media_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_media_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _media_skeleton_vtable =
{
  _media_skeleton_handle_method_call,
  _media_skeleton_handle_get_property,
  _media_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
media_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return media_interface_info ();
}

static GDBusInterfaceVTable *
media_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_media_skeleton_vtable;
}

static GVariant *
media_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_media_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _media_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _media_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _media_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _media_emit_changed (gpointer user_data);

static void
media_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _media_emit_changed (skeleton);
}

static void
_media_on_signal_play_attributes_changed (
    Media *object,
    const gchar *arg_title,
    const gchar *arg_artist,
    const gchar *arg_album,
    const gchar *arg_genre,
    gint arg_trackNumber)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ssssi)",
                   arg_title,
                   arg_artist,
                   arg_album,
                   arg_genre,
                   arg_trackNumber));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", "PlayAttributesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_media_on_signal_play_status_changed (
    Media *object,
    gint arg_playState,
    const gchar *arg_songLength,
    const gchar *arg_playPosition)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(iss)",
                   arg_playState,
                   arg_songLength,
                   arg_playPosition));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", "PlayStatusChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_media_on_signal_stream_status_changed (
    Media *object,
    gboolean arg_streamStatus)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_streamStatus));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", "streamStatusChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_media_on_signal_capability_changed (
    Media *object,
    gint arg_ncount,
    const gchar *arg_eventname)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(i^ay)",
                   arg_ncount,
                   arg_eventname));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", "capabilityChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_media_on_signal_playermodes_changed (
    Media *object,
    gint arg_shuffle,
    gint arg_repeate)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ii)",
                   arg_shuffle,
                   arg_repeate));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.media", "playermodesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void media_skeleton_iface_init (MediaIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (MediaSkeleton, media_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (MediaSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIA, media_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (MediaSkeleton, media_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_MEDIA, media_skeleton_iface_init));

#endif
static void
media_skeleton_finalize (GObject *object)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  guint n;
  for (n = 0; n < 3; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (media_skeleton_parent_class)->finalize (object);
}

static void
media_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_media_emit_changed (gpointer user_data)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.hsaeyz.bluetooth.media",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_media_schedule_emit_changed (MediaSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
media_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _media_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
media_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 3);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _media_schedule_emit_changed (skeleton, _media_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
media_skeleton_init (MediaSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = media_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_MEDIA_SKELETON, MediaSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 3);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_INT);
}

static gboolean 
media_skeleton_get_music_mute_state (Media *object)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
media_skeleton_get_shufflemode (Media *object)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
media_skeleton_get_reaptemode (Media *object)
{
  MediaSkeleton *skeleton = MEDIA_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
media_skeleton_class_init (MediaSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = media_skeleton_finalize;
  gobject_class->get_property = media_skeleton_get_property;
  gobject_class->set_property = media_skeleton_set_property;
  gobject_class->notify       = media_skeleton_notify;


  media_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = media_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = media_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = media_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = media_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (MediaSkeletonPrivate));
#endif
}

static void
media_skeleton_iface_init (MediaIface *iface)
{
  iface->play_attributes_changed = _media_on_signal_play_attributes_changed;
  iface->play_status_changed = _media_on_signal_play_status_changed;
  iface->stream_status_changed = _media_on_signal_stream_status_changed;
  iface->capability_changed = _media_on_signal_capability_changed;
  iface->playermodes_changed = _media_on_signal_playermodes_changed;
  iface->get_music_mute_state = media_skeleton_get_music_mute_state;
  iface->get_shufflemode = media_skeleton_get_shufflemode;
  iface->get_reaptemode = media_skeleton_get_reaptemode;
}

/**
 * media_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-media.top_of_page">com.hsaeyz.bluetooth.media</link>.
 *
 * Returns: (transfer full) (type MediaSkeleton): The skeleton object.
 */
Media *
media_skeleton_new (void)
{
  return MEDIA (g_object_new (TYPE_MEDIA_SKELETON, NULL));
}

