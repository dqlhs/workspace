/*
 * Generated by gdbus-codegen 2.40.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "set.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsaeyz.bluetooth.set
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Set
 * @title: Set
 * @short_description: Generated C code for the com.hsaeyz.bluetooth.set D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsaeyz.bluetooth.set ---- */

static const _ExtendedGDBusArgInfo _set_method_info_set_power_IN_ARG_power =
{
  {
    -1,
    (gchar *) "power",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_power_IN_ARG_pointers[] =
{
  &_set_method_info_set_power_IN_ARG_power,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_power =
{
  {
    -1,
    (gchar *) "setPower",
    (GDBusArgInfo **) &_set_method_info_set_power_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-power",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_setbt_power_IN_ARG_power =
{
  {
    -1,
    (gchar *) "power",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_setbt_power_IN_ARG_pointers[] =
{
  &_set_method_info_setbt_power_IN_ARG_power,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_setbt_power =
{
  {
    -1,
    (gchar *) "setbtPower",
    (GDBusArgInfo **) &_set_method_info_setbt_power_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-setbt-power",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_set_scan_enable_IN_ARG_scan =
{
  {
    -1,
    (gchar *) "scan",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_scan_enable_IN_ARG_pointers[] =
{
  &_set_method_info_set_scan_enable_IN_ARG_scan,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_scan_enable =
{
  {
    -1,
    (gchar *) "setScanEnable",
    (GDBusArgInfo **) &_set_method_info_set_scan_enable_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-scan-enable",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_set_local_device_name_IN_ARG_name =
{
  {
    -1,
    (gchar *) "name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_local_device_name_IN_ARG_pointers[] =
{
  &_set_method_info_set_local_device_name_IN_ARG_name,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_local_device_name =
{
  {
    -1,
    (gchar *) "setLocalDeviceName",
    (GDBusArgInfo **) &_set_method_info_set_local_device_name_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-local-device-name",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_set_auto_connect_IN_ARG_autoconnect =
{
  {
    -1,
    (gchar *) "autoconnect",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_auto_connect_IN_ARG_pointers[] =
{
  &_set_method_info_set_auto_connect_IN_ARG_autoconnect,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_auto_connect =
{
  {
    -1,
    (gchar *) "setAutoConnect",
    (GDBusArgInfo **) &_set_method_info_set_auto_connect_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-auto-connect",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_enable_auto_connect_IN_ARG_enableconnect =
{
  {
    -1,
    (gchar *) "enableconnect",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_enable_auto_connect_IN_ARG_pointers[] =
{
  &_set_method_info_enable_auto_connect_IN_ARG_enableconnect,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_enable_auto_connect =
{
  {
    -1,
    (gchar *) "enableAutoConnect",
    (GDBusArgInfo **) &_set_method_info_enable_auto_connect_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-enable-auto-connect",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_set_auto_answer_IN_ARG_autoanswer =
{
  {
    -1,
    (gchar *) "autoanswer",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_auto_answer_IN_ARG_pointers[] =
{
  &_set_method_info_set_auto_answer_IN_ARG_autoanswer,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_auto_answer =
{
  {
    -1,
    (gchar *) "setAutoAnswer",
    (GDBusArgInfo **) &_set_method_info_set_auto_answer_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-auto-answer",
  FALSE
};

static const _ExtendedGDBusArgInfo _set_method_info_set_scan_time_IN_ARG_time =
{
  {
    -1,
    (gchar *) "time",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_method_info_set_scan_time_IN_ARG_pointers[] =
{
  &_set_method_info_set_scan_time_IN_ARG_time,
  NULL
};

static const _ExtendedGDBusMethodInfo _set_method_info_set_scan_time =
{
  {
    -1,
    (gchar *) "setScanTime",
    (GDBusArgInfo **) &_set_method_info_set_scan_time_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-scan-time",
  FALSE
};

static const _ExtendedGDBusMethodInfo _set_method_info_factoryreset =
{
  {
    -1,
    (gchar *) "factoryreset",
    NULL,
    NULL,
    NULL
  },
  "handle-factoryreset",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _set_method_info_pointers[] =
{
  &_set_method_info_set_power,
  &_set_method_info_setbt_power,
  &_set_method_info_set_scan_enable,
  &_set_method_info_set_local_device_name,
  &_set_method_info_set_auto_connect,
  &_set_method_info_enable_auto_connect,
  &_set_method_info_set_auto_answer,
  &_set_method_info_set_scan_time,
  &_set_method_info_factoryreset,
  NULL
};

static const _ExtendedGDBusArgInfo _set_signal_info_power_state_changed_ARG_powerState =
{
  {
    -1,
    (gchar *) "powerState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_signal_info_power_state_changed_ARG_pointers[] =
{
  &_set_signal_info_power_state_changed_ARG_powerState,
  NULL
};

static const _ExtendedGDBusSignalInfo _set_signal_info_power_state_changed =
{
  {
    -1,
    (gchar *) "PowerStateChanged",
    (GDBusArgInfo **) &_set_signal_info_power_state_changed_ARG_pointers,
    NULL
  },
  "power-state-changed"
};

static const _ExtendedGDBusArgInfo _set_signal_info_auto_state_changed_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_signal_info_auto_state_changed_ARG_pointers[] =
{
  &_set_signal_info_auto_state_changed_ARG_state,
  NULL
};

static const _ExtendedGDBusSignalInfo _set_signal_info_auto_state_changed =
{
  {
    -1,
    (gchar *) "autoStateChanged",
    (GDBusArgInfo **) &_set_signal_info_auto_state_changed_ARG_pointers,
    NULL
  },
  "auto-state-changed"
};

static const _ExtendedGDBusArgInfo _set_signal_info_version_state_changed_ARG_verson =
{
  {
    -1,
    (gchar *) "verson",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _set_signal_info_version_state_changed_ARG_pointers[] =
{
  &_set_signal_info_version_state_changed_ARG_verson,
  NULL
};

static const _ExtendedGDBusSignalInfo _set_signal_info_version_state_changed =
{
  {
    -1,
    (gchar *) "versionStateChanged",
    (GDBusArgInfo **) &_set_signal_info_version_state_changed_ARG_pointers,
    NULL
  },
  "version-state-changed"
};

static const _ExtendedGDBusSignalInfo * const _set_signal_info_pointers[] =
{
  &_set_signal_info_power_state_changed,
  &_set_signal_info_auto_state_changed,
  &_set_signal_info_version_state_changed,
  NULL
};

static const _ExtendedGDBusPropertyInfo _set_property_info_auto_state =
{
  {
    -1,
    (gchar *) "autoState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "auto-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _set_property_info_btversion =
{
  {
    -1,
    (gchar *) "btversion",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "btversion",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _set_property_info_local_address =
{
  {
    -1,
    (gchar *) "LocalAddress",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "local-address",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _set_property_info_local_name =
{
  {
    -1,
    (gchar *) "LocalName",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "local-name",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _set_property_info_power_state =
{
  {
    -1,
    (gchar *) "PowerState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "power-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _set_property_info_pointers[] =
{
  &_set_property_info_auto_state,
  &_set_property_info_btversion,
  &_set_property_info_local_address,
  &_set_property_info_local_name,
  &_set_property_info_power_state,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _set_interface_info =
{
  {
    -1,
    (gchar *) "com.hsaeyz.bluetooth.set",
    (GDBusMethodInfo **) &_set_method_info_pointers,
    (GDBusSignalInfo **) &_set_signal_info_pointers,
    (GDBusPropertyInfo **) &_set_property_info_pointers,
    NULL
  },
  "set",
};


/**
 * set_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
set_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_set_interface_info.parent_struct;
}

/**
 * set_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Set interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
set_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "auto-state");
  g_object_class_override_property (klass, property_id_begin++, "btversion");
  g_object_class_override_property (klass, property_id_begin++, "local-address");
  g_object_class_override_property (klass, property_id_begin++, "local-name");
  g_object_class_override_property (klass, property_id_begin++, "power-state");
  return property_id_begin - 1;
}



/**
 * Set:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link>.
 */

/**
 * SetIface:
 * @parent_iface: The parent interface.
 * @handle_enable_auto_connect: Handler for the #Set::handle-enable-auto-connect signal.
 * @handle_factoryreset: Handler for the #Set::handle-factoryreset signal.
 * @handle_set_auto_answer: Handler for the #Set::handle-set-auto-answer signal.
 * @handle_set_auto_connect: Handler for the #Set::handle-set-auto-connect signal.
 * @handle_set_local_device_name: Handler for the #Set::handle-set-local-device-name signal.
 * @handle_set_power: Handler for the #Set::handle-set-power signal.
 * @handle_set_scan_enable: Handler for the #Set::handle-set-scan-enable signal.
 * @handle_set_scan_time: Handler for the #Set::handle-set-scan-time signal.
 * @handle_setbt_power: Handler for the #Set::handle-setbt-power signal.
 * @get_auto_state: Getter for the #Set:auto-state property.
 * @get_btversion: Getter for the #Set:btversion property.
 * @get_local_address: Getter for the #Set:local-address property.
 * @get_local_name: Getter for the #Set:local-name property.
 * @get_power_state: Getter for the #Set:power-state property.
 * @auto_state_changed: Handler for the #Set::auto-state-changed signal.
 * @power_state_changed: Handler for the #Set::power-state-changed signal.
 * @version_state_changed: Handler for the #Set::version-state-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link>.
 */

typedef SetIface SetInterface;
G_DEFINE_INTERFACE (Set, set, G_TYPE_OBJECT);

static void
set_default_init (SetIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Set::handle-set-power:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_power: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setPower">setPower()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_power() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-power",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_power),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-setbt-power:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_power: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setbtPower">setbtPower()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_setbt_power() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-setbt-power",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_setbt_power),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-set-scan-enable:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_scan: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanEnable">setScanEnable()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_scan_enable() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-scan-enable",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_scan_enable),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-set-local-device-name:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setLocalDeviceName">setLocalDeviceName()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_local_device_name() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-local-device-name",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_local_device_name),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * Set::handle-set-auto-connect:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_autoconnect: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoConnect">setAutoConnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_auto_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-auto-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_auto_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-enable-auto-connect:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_enableconnect: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.enableAutoConnect">enableAutoConnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_enable_auto_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-enable-auto-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_enable_auto_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-set-auto-answer:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_autoanswer: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoAnswer">setAutoAnswer()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_auto_answer() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-auto-answer",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_auto_answer),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_BOOLEAN);

  /**
   * Set::handle-set-scan-time:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_time: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanTime">setScanTime()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_set_scan_time() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-scan-time",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_set_scan_time),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT);

  /**
   * Set::handle-factoryreset:
   * @object: A #Set.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.factoryreset">factoryreset()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call set_complete_factoryreset() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-factoryreset",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, handle_factoryreset),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * Set::power-state-changed:
   * @object: A #Set.
   * @arg_powerState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.PowerStateChanged">"PowerStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("power-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, power_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /**
   * Set::auto-state-changed:
   * @object: A #Set.
   * @arg_state: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.autoStateChanged">"autoStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("auto-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, auto_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /**
   * Set::version-state-changed:
   * @object: A #Set.
   * @arg_verson: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.versionStateChanged">"versionStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("version-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (SetIface, version_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /* GObject properties for D-Bus properties: */
  /**
   * Set:auto-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.autoState">"autoState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("auto-state", "autoState", "autoState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Set:btversion:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.btversion">"btversion"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("btversion", "btversion", "btversion", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Set:local-address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalAddress">"LocalAddress"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("local-address", "LocalAddress", "LocalAddress", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Set:local-name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalName">"LocalName"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("local-name", "LocalName", "LocalName", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Set:power-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.PowerState">"PowerState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("power-state", "PowerState", "PowerState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * set_get_auto_state: (skip)
 * @object: A #Set.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.autoState">"autoState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
set_get_auto_state (Set *object)
{
  return SET_GET_IFACE (object)->get_auto_state (object);
}

/**
 * set_set_auto_state: (skip)
 * @object: A #Set.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.autoState">"autoState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
set_set_auto_state (Set *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "auto-state", value, NULL);
}

/**
 * set_get_btversion: (skip)
 * @object: A #Set.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.btversion">"btversion"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use set_dup_btversion() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
set_get_btversion (Set *object)
{
  return SET_GET_IFACE (object)->get_btversion (object);
}

/**
 * set_dup_btversion: (skip)
 * @object: A #Set.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.btversion">"btversion"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
set_dup_btversion (Set *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "btversion", &value, NULL);
  return value;
}

/**
 * set_set_btversion: (skip)
 * @object: A #Set.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.btversion">"btversion"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
set_set_btversion (Set *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "btversion", value, NULL);
}

/**
 * set_get_local_address: (skip)
 * @object: A #Set.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalAddress">"LocalAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use set_dup_local_address() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
set_get_local_address (Set *object)
{
  return SET_GET_IFACE (object)->get_local_address (object);
}

/**
 * set_dup_local_address: (skip)
 * @object: A #Set.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalAddress">"LocalAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
set_dup_local_address (Set *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "local-address", &value, NULL);
  return value;
}

/**
 * set_set_local_address: (skip)
 * @object: A #Set.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalAddress">"LocalAddress"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
set_set_local_address (Set *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "local-address", value, NULL);
}

/**
 * set_get_local_name: (skip)
 * @object: A #Set.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use set_dup_local_name() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
set_get_local_name (Set *object)
{
  return SET_GET_IFACE (object)->get_local_name (object);
}

/**
 * set_dup_local_name: (skip)
 * @object: A #Set.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
set_dup_local_name (Set *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "local-name", &value, NULL);
  return value;
}

/**
 * set_set_local_name: (skip)
 * @object: A #Set.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.LocalName">"LocalName"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
set_set_local_name (Set *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "local-name", value, NULL);
}

/**
 * set_get_power_state: (skip)
 * @object: A #Set.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.PowerState">"PowerState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
set_get_power_state (Set *object)
{
  return SET_GET_IFACE (object)->get_power_state (object);
}

/**
 * set_set_power_state: (skip)
 * @object: A #Set.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-set.PowerState">"PowerState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
set_set_power_state (Set *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "power-state", value, NULL);
}

/**
 * set_emit_power_state_changed:
 * @object: A #Set.
 * @arg_powerState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.PowerStateChanged">"PowerStateChanged"</link> D-Bus signal.
 */
void
set_emit_power_state_changed (
    Set *object,
    gboolean arg_powerState)
{
  g_signal_emit_by_name (object, "power-state-changed", arg_powerState);
}

/**
 * set_emit_auto_state_changed:
 * @object: A #Set.
 * @arg_state: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.autoStateChanged">"autoStateChanged"</link> D-Bus signal.
 */
void
set_emit_auto_state_changed (
    Set *object,
    gboolean arg_state)
{
  g_signal_emit_by_name (object, "auto-state-changed", arg_state);
}

/**
 * set_emit_version_state_changed:
 * @object: A #Set.
 * @arg_verson: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-set.versionStateChanged">"versionStateChanged"</link> D-Bus signal.
 */
void
set_emit_version_state_changed (
    Set *object,
    const gchar *arg_verson)
{
  g_signal_emit_by_name (object, "version-state-changed", arg_verson);
}

/**
 * set_call_set_power:
 * @proxy: A #SetProxy.
 * @arg_power: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setPower">setPower()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_power_finish() to get the result of the operation.
 *
 * See set_call_set_power_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_power (
    Set *proxy,
    gboolean arg_power,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setPower",
    g_variant_new ("(b)",
                   arg_power),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_power_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_power().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_power().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_power_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_power_sync:
 * @proxy: A #SetProxy.
 * @arg_power: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setPower">setPower()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_power() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_power_sync (
    Set *proxy,
    gboolean arg_power,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setPower",
    g_variant_new ("(b)",
                   arg_power),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_setbt_power:
 * @proxy: A #SetProxy.
 * @arg_power: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setbtPower">setbtPower()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_setbt_power_finish() to get the result of the operation.
 *
 * See set_call_setbt_power_sync() for the synchronous, blocking version of this method.
 */
void
set_call_setbt_power (
    Set *proxy,
    gboolean arg_power,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setbtPower",
    g_variant_new ("(b)",
                   arg_power),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_setbt_power_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_setbt_power().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_setbt_power().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_setbt_power_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_setbt_power_sync:
 * @proxy: A #SetProxy.
 * @arg_power: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setbtPower">setbtPower()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_setbt_power() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_setbt_power_sync (
    Set *proxy,
    gboolean arg_power,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setbtPower",
    g_variant_new ("(b)",
                   arg_power),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_scan_enable:
 * @proxy: A #SetProxy.
 * @arg_scan: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanEnable">setScanEnable()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_scan_enable_finish() to get the result of the operation.
 *
 * See set_call_set_scan_enable_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_scan_enable (
    Set *proxy,
    gboolean arg_scan,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setScanEnable",
    g_variant_new ("(b)",
                   arg_scan),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_scan_enable_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_scan_enable().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_scan_enable().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_scan_enable_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_scan_enable_sync:
 * @proxy: A #SetProxy.
 * @arg_scan: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanEnable">setScanEnable()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_scan_enable() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_scan_enable_sync (
    Set *proxy,
    gboolean arg_scan,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setScanEnable",
    g_variant_new ("(b)",
                   arg_scan),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_local_device_name:
 * @proxy: A #SetProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setLocalDeviceName">setLocalDeviceName()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_local_device_name_finish() to get the result of the operation.
 *
 * See set_call_set_local_device_name_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_local_device_name (
    Set *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setLocalDeviceName",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_local_device_name_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_local_device_name().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_local_device_name().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_local_device_name_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_local_device_name_sync:
 * @proxy: A #SetProxy.
 * @arg_name: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setLocalDeviceName">setLocalDeviceName()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_local_device_name() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_local_device_name_sync (
    Set *proxy,
    const gchar *arg_name,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setLocalDeviceName",
    g_variant_new ("(s)",
                   arg_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_auto_connect:
 * @proxy: A #SetProxy.
 * @arg_autoconnect: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoConnect">setAutoConnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_auto_connect_finish() to get the result of the operation.
 *
 * See set_call_set_auto_connect_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_auto_connect (
    Set *proxy,
    gboolean arg_autoconnect,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setAutoConnect",
    g_variant_new ("(b)",
                   arg_autoconnect),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_auto_connect_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_auto_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_auto_connect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_auto_connect_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_auto_connect_sync:
 * @proxy: A #SetProxy.
 * @arg_autoconnect: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoConnect">setAutoConnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_auto_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_auto_connect_sync (
    Set *proxy,
    gboolean arg_autoconnect,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setAutoConnect",
    g_variant_new ("(b)",
                   arg_autoconnect),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_enable_auto_connect:
 * @proxy: A #SetProxy.
 * @arg_enableconnect: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.enableAutoConnect">enableAutoConnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_enable_auto_connect_finish() to get the result of the operation.
 *
 * See set_call_enable_auto_connect_sync() for the synchronous, blocking version of this method.
 */
void
set_call_enable_auto_connect (
    Set *proxy,
    gboolean arg_enableconnect,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "enableAutoConnect",
    g_variant_new ("(b)",
                   arg_enableconnect),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_enable_auto_connect_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_enable_auto_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_enable_auto_connect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_enable_auto_connect_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_enable_auto_connect_sync:
 * @proxy: A #SetProxy.
 * @arg_enableconnect: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.enableAutoConnect">enableAutoConnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_enable_auto_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_enable_auto_connect_sync (
    Set *proxy,
    gboolean arg_enableconnect,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "enableAutoConnect",
    g_variant_new ("(b)",
                   arg_enableconnect),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_auto_answer:
 * @proxy: A #SetProxy.
 * @arg_autoanswer: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoAnswer">setAutoAnswer()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_auto_answer_finish() to get the result of the operation.
 *
 * See set_call_set_auto_answer_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_auto_answer (
    Set *proxy,
    gboolean arg_autoanswer,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setAutoAnswer",
    g_variant_new ("(b)",
                   arg_autoanswer),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_auto_answer_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_auto_answer().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_auto_answer().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_auto_answer_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_auto_answer_sync:
 * @proxy: A #SetProxy.
 * @arg_autoanswer: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoAnswer">setAutoAnswer()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_auto_answer() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_auto_answer_sync (
    Set *proxy,
    gboolean arg_autoanswer,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setAutoAnswer",
    g_variant_new ("(b)",
                   arg_autoanswer),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_scan_time:
 * @proxy: A #SetProxy.
 * @arg_time: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanTime">setScanTime()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_set_scan_time_finish() to get the result of the operation.
 *
 * See set_call_set_scan_time_sync() for the synchronous, blocking version of this method.
 */
void
set_call_set_scan_time (
    Set *proxy,
    gint arg_time,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setScanTime",
    g_variant_new ("(i)",
                   arg_time),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_set_scan_time_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_set_scan_time().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_set_scan_time().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_scan_time_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_set_scan_time_sync:
 * @proxy: A #SetProxy.
 * @arg_time: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanTime">setScanTime()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_set_scan_time() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_set_scan_time_sync (
    Set *proxy,
    gint arg_time,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setScanTime",
    g_variant_new ("(i)",
                   arg_time),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_factoryreset:
 * @proxy: A #SetProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.factoryreset">factoryreset()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_call_factoryreset_finish() to get the result of the operation.
 *
 * See set_call_factoryreset_sync() for the synchronous, blocking version of this method.
 */
void
set_call_factoryreset (
    Set *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "factoryreset",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * set_call_factoryreset_finish:
 * @proxy: A #SetProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_call_factoryreset().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with set_call_factoryreset().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_factoryreset_finish (
    Set *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_call_factoryreset_sync:
 * @proxy: A #SetProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.factoryreset">factoryreset()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See set_call_factoryreset() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
set_call_factoryreset_sync (
    Set *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "factoryreset",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * set_complete_set_power:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setPower">setPower()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_power (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_setbt_power:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setbtPower">setbtPower()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_setbt_power (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_set_scan_enable:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanEnable">setScanEnable()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_scan_enable (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_set_local_device_name:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setLocalDeviceName">setLocalDeviceName()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_local_device_name (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_set_auto_connect:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoConnect">setAutoConnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_auto_connect (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_enable_auto_connect:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.enableAutoConnect">enableAutoConnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_enable_auto_connect (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_set_auto_answer:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setAutoAnswer">setAutoAnswer()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_auto_answer (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_set_scan_time:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.setScanTime">setScanTime()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_set_scan_time (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * set_complete_factoryreset:
 * @object: A #Set.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-set.factoryreset">factoryreset()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
set_complete_factoryreset (
    Set *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * SetProxy:
 *
 * The #SetProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * SetProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #SetProxy.
 */

struct _SetProxyPrivate
{
  GData *qdata;
};

static void set_proxy_iface_init (SetIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (SetProxy, set_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (SetProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_SET, set_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (SetProxy, set_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_SET, set_proxy_iface_init));

#endif
static void
set_proxy_finalize (GObject *object)
{
  SetProxy *proxy = SET_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (set_proxy_parent_class)->finalize (object);
}

static void
set_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = _set_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
set_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.hsaeyz.bluetooth.set: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
set_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  info = _set_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.hsaeyz.bluetooth.set", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) set_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
set_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_set_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_SET);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_SET);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
set_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  SetProxy *proxy = SET_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_set_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_set_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
set_proxy_get_auto_state (Set *object)
{
  SetProxy *proxy = SET_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "autoState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
set_proxy_get_btversion (Set *object)
{
  SetProxy *proxy = SET_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "btversion");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
set_proxy_get_local_address (Set *object)
{
  SetProxy *proxy = SET_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LocalAddress");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
set_proxy_get_local_name (Set *object)
{
  SetProxy *proxy = SET_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LocalName");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
set_proxy_get_power_state (Set *object)
{
  SetProxy *proxy = SET_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PowerState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
set_proxy_init (SetProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = set_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_SET_PROXY, SetProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), set_interface_info ());
}

static void
set_proxy_class_init (SetProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = set_proxy_finalize;
  gobject_class->get_property = set_proxy_get_property;
  gobject_class->set_property = set_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = set_proxy_g_signal;
  proxy_class->g_properties_changed = set_proxy_g_properties_changed;

  set_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (SetProxyPrivate));
#endif
}

static void
set_proxy_iface_init (SetIface *iface)
{
  iface->get_auto_state = set_proxy_get_auto_state;
  iface->get_btversion = set_proxy_get_btversion;
  iface->get_local_address = set_proxy_get_local_address;
  iface->get_local_name = set_proxy_get_local_name;
  iface->get_power_state = set_proxy_get_power_state;
}

/**
 * set_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_proxy_new_finish() to get the result of the operation.
 *
 * See set_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
set_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_SET_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.set", NULL);
}

/**
 * set_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with set_proxy_new().
 *
 * Returns: (transfer full) (type SetProxy): The constructed proxy object or %NULL if @error is set.
 */
Set *
set_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SET (ret);
  else
    return NULL;
}

/**
 * set_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See set_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type SetProxy): The constructed proxy object or %NULL if @error is set.
 */
Set *
set_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_SET_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.set", NULL);
  if (ret != NULL)
    return SET (ret);
  else
    return NULL;
}


/**
 * set_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like set_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call set_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See set_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
set_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_SET_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.set", NULL);
}

/**
 * set_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to set_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with set_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type SetProxy): The constructed proxy object or %NULL if @error is set.
 */
Set *
set_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return SET (ret);
  else
    return NULL;
}

/**
 * set_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like set_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See set_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type SetProxy): The constructed proxy object or %NULL if @error is set.
 */
Set *
set_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_SET_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.set", NULL);
  if (ret != NULL)
    return SET (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * SetSkeleton:
 *
 * The #SetSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * SetSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #SetSkeleton.
 */

struct _SetSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_set_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  SetSkeleton *skeleton = SET_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_SET);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_SET);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_set_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  SetSkeleton *skeleton = SET_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_set_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_set_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  SetSkeleton *skeleton = SET_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_set_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _set_skeleton_vtable =
{
  _set_skeleton_handle_method_call,
  _set_skeleton_handle_get_property,
  _set_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
set_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return set_interface_info ();
}

static GDBusInterfaceVTable *
set_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_set_skeleton_vtable;
}

static GVariant *
set_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  SetSkeleton *skeleton = SET_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_set_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _set_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _set_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _set_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.set", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _set_emit_changed (gpointer user_data);

static void
set_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  SetSkeleton *skeleton = SET_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _set_emit_changed (skeleton);
}

static void
_set_on_signal_power_state_changed (
    Set *object,
    gboolean arg_powerState)
{
  SetSkeleton *skeleton = SET_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_powerState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.set", "PowerStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_set_on_signal_auto_state_changed (
    Set *object,
    gboolean arg_state)
{
  SetSkeleton *skeleton = SET_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_state));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.set", "autoStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_set_on_signal_version_state_changed (
    Set *object,
    const gchar *arg_verson)
{
  SetSkeleton *skeleton = SET_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_verson));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.set", "versionStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void set_skeleton_iface_init (SetIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (SetSkeleton, set_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (SetSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_SET, set_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (SetSkeleton, set_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_SET, set_skeleton_iface_init));

#endif
static void
set_skeleton_finalize (GObject *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  guint n;
  for (n = 0; n < 5; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (set_skeleton_parent_class)->finalize (object);
}

static void
set_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_set_emit_changed (gpointer user_data)
{
  SetSkeleton *skeleton = SET_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.hsaeyz.bluetooth.set",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_set_schedule_emit_changed (SetSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
set_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _set_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
set_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 5);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _set_schedule_emit_changed (skeleton, _set_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
set_skeleton_init (SetSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = set_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_SET_SKELETON, SetSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 5);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
}

static gboolean 
set_skeleton_get_auto_state (Set *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
set_skeleton_get_btversion (Set *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
set_skeleton_get_local_address (Set *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
set_skeleton_get_local_name (Set *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
set_skeleton_get_power_state (Set *object)
{
  SetSkeleton *skeleton = SET_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
set_skeleton_class_init (SetSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = set_skeleton_finalize;
  gobject_class->get_property = set_skeleton_get_property;
  gobject_class->set_property = set_skeleton_set_property;
  gobject_class->notify       = set_skeleton_notify;


  set_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = set_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = set_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = set_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = set_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (SetSkeletonPrivate));
#endif
}

static void
set_skeleton_iface_init (SetIface *iface)
{
  iface->power_state_changed = _set_on_signal_power_state_changed;
  iface->auto_state_changed = _set_on_signal_auto_state_changed;
  iface->version_state_changed = _set_on_signal_version_state_changed;
  iface->get_auto_state = set_skeleton_get_auto_state;
  iface->get_btversion = set_skeleton_get_btversion;
  iface->get_local_address = set_skeleton_get_local_address;
  iface->get_local_name = set_skeleton_get_local_name;
  iface->get_power_state = set_skeleton_get_power_state;
}

/**
 * set_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-set.top_of_page">com.hsaeyz.bluetooth.set</link>.
 *
 * Returns: (transfer full) (type SetSkeleton): The skeleton object.
 */
Set *
set_skeleton_new (void)
{
  return SET (g_object_new (TYPE_SET_SKELETON, NULL));
}

