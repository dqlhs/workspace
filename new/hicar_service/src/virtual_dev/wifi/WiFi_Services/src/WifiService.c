/*
 * Generated by gdbus-codegen 2.58.1 from WifiService.xml. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "WifiService.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsaeyz.sal.WifiService
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:WifiService
 * @title: WifiService
 * @short_description: Generated C code for the com.hsaeyz.sal.WifiService D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsaeyz.sal.WifiService ---- */

static const _ExtendedGDBusArgInfo _wifi_service_method_info_set_wifi_enabled_IN_ARG_enabled =
{
  {
    -1,
    (gchar *) "enabled",
    (gchar *) "(b)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_set_wifi_enabled_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_set_wifi_enabled_IN_ARG_enabled.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_set_wifi_enabled =
{
  {
    -1,
    (gchar *) "setWifiEnabled",
    (GDBusArgInfo **) &_wifi_service_method_info_set_wifi_enabled_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-wifi-enabled",
  FALSE
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_start_scan =
{
  {
    -1,
    (gchar *) "startScan",
    NULL,
    NULL,
    NULL
  },
  "handle-start-scan",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_get_scan_results_OUT_ARG_results =
{
  {
    -1,
    (gchar *) "results",
    (gchar *) "a(ssiiiasb)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_get_scan_results_OUT_ARG_pointers[] =
{
  &_wifi_service_method_info_get_scan_results_OUT_ARG_results.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_get_scan_results =
{
  {
    -1,
    (gchar *) "getScanResults",
    NULL,
    (GDBusArgInfo **) &_wifi_service_method_info_get_scan_results_OUT_ARG_pointers,
    NULL
  },
  "handle-get-scan-results",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_connect_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(ss)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_connect_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_connect_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_connect =
{
  {
    -1,
    (gchar *) "connect",
    (GDBusArgInfo **) &_wifi_service_method_info_connect_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-connect",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_update_host_ap_mac_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(s)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_update_host_ap_mac_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_update_host_ap_mac_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_update_host_ap_mac =
{
  {
    -1,
    (gchar *) "updateHostApMac",
    (GDBusArgInfo **) &_wifi_service_method_info_update_host_ap_mac_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-update-host-ap-mac",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_addsta_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(s)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_addsta_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_addsta_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_addsta =
{
  {
    -1,
    (gchar *) "addsta",
    (GDBusArgInfo **) &_wifi_service_method_info_addsta_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-addsta",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_deletesta_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(s)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_deletesta_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_deletesta_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_deletesta =
{
  {
    -1,
    (gchar *) "deletesta",
    (GDBusArgInfo **) &_wifi_service_method_info_deletesta_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-deletesta",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_start_host_ap_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(ss)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_start_host_ap_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_start_host_ap_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_start_host_ap =
{
  {
    -1,
    (gchar *) "startHostAp",
    (GDBusArgInfo **) &_wifi_service_method_info_start_host_ap_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-start-host-ap",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_stop_host_ap_IN_ARG_config =
{
  {
    -1,
    (gchar *) "config",
    (gchar *) "(ss)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_stop_host_ap_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_stop_host_ap_IN_ARG_config.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_stop_host_ap =
{
  {
    -1,
    (gchar *) "stopHostAp",
    (GDBusArgInfo **) &_wifi_service_method_info_stop_host_ap_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-stop-host-ap",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_get_host_ap_client_info_OUT_ARG_info =
{
  {
    -1,
    (gchar *) "info",
    (gchar *) "(ssiisiiiss)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_get_host_ap_client_info_OUT_ARG_pointers[] =
{
  &_wifi_service_method_info_get_host_ap_client_info_OUT_ARG_info.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_get_host_ap_client_info =
{
  {
    -1,
    (gchar *) "getHostApClientInfo",
    NULL,
    (GDBusArgInfo **) &_wifi_service_method_info_get_host_ap_client_info_OUT_ARG_pointers,
    NULL
  },
  "handle-get-host-ap-client-info",
  FALSE
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_disconnect =
{
  {
    -1,
    (gchar *) "disconnect",
    NULL,
    NULL,
    NULL
  },
  "handle-disconnect",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_set_periodic_scan_interval_IN_ARG_interval =
{
  {
    -1,
    (gchar *) "interval",
    (gchar *) "(i)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_set_periodic_scan_interval_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_set_periodic_scan_interval_IN_ARG_interval.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_set_periodic_scan_interval =
{
  {
    -1,
    (gchar *) "setPeriodicScanInterval",
    (GDBusArgInfo **) &_wifi_service_method_info_set_periodic_scan_interval_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-periodic-scan-interval",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_is_wifi_enabled_OUT_ARG_enabled =
{
  {
    -1,
    (gchar *) "enabled",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_is_wifi_enabled_OUT_ARG_pointers[] =
{
  &_wifi_service_method_info_is_wifi_enabled_OUT_ARG_enabled.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_is_wifi_enabled =
{
  {
    -1,
    (gchar *) "isWifiEnabled",
    NULL,
    (GDBusArgInfo **) &_wifi_service_method_info_is_wifi_enabled_OUT_ARG_pointers,
    NULL
  },
  "handle-is-wifi-enabled",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_get_connection_info_OUT_ARG_info =
{
  {
    -1,
    (gchar *) "info",
    (gchar *) "(ssiisiiiss)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_get_connection_info_OUT_ARG_pointers[] =
{
  &_wifi_service_method_info_get_connection_info_OUT_ARG_info.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_get_connection_info =
{
  {
    -1,
    (gchar *) "getConnectionInfo",
    NULL,
    (GDBusArgInfo **) &_wifi_service_method_info_get_connection_info_OUT_ARG_pointers,
    NULL
  },
  "handle-get-connection-info",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_get_saved_networks_OUT_ARG_networks =
{
  {
    -1,
    (gchar *) "networks",
    (gchar *) "a(issibb)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_get_saved_networks_OUT_ARG_pointers[] =
{
  &_wifi_service_method_info_get_saved_networks_OUT_ARG_networks.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_get_saved_networks =
{
  {
    -1,
    (gchar *) "getSavedNetworks",
    NULL,
    (GDBusArgInfo **) &_wifi_service_method_info_get_saved_networks_OUT_ARG_pointers,
    NULL
  },
  "handle-get-saved-networks",
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_method_info_select_network_IN_ARG_netid =
{
  {
    -1,
    (gchar *) "netid",
    (gchar *) "(i)",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_method_info_select_network_IN_ARG_pointers[] =
{
  &_wifi_service_method_info_select_network_IN_ARG_netid.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _wifi_service_method_info_select_network =
{
  {
    -1,
    (gchar *) "selectNetwork",
    (GDBusArgInfo **) &_wifi_service_method_info_select_network_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-select-network",
  FALSE
};

static const GDBusMethodInfo * const _wifi_service_method_info_pointers[] =
{
  &_wifi_service_method_info_set_wifi_enabled.parent_struct,
  &_wifi_service_method_info_start_scan.parent_struct,
  &_wifi_service_method_info_get_scan_results.parent_struct,
  &_wifi_service_method_info_connect.parent_struct,
  &_wifi_service_method_info_update_host_ap_mac.parent_struct,
  &_wifi_service_method_info_addsta.parent_struct,
  &_wifi_service_method_info_deletesta.parent_struct,
  &_wifi_service_method_info_start_host_ap.parent_struct,
  &_wifi_service_method_info_stop_host_ap.parent_struct,
  &_wifi_service_method_info_get_host_ap_client_info.parent_struct,
  &_wifi_service_method_info_disconnect.parent_struct,
  &_wifi_service_method_info_set_periodic_scan_interval.parent_struct,
  &_wifi_service_method_info_is_wifi_enabled.parent_struct,
  &_wifi_service_method_info_get_connection_info.parent_struct,
  &_wifi_service_method_info_get_saved_networks.parent_struct,
  &_wifi_service_method_info_select_network.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wifi_service_signal_info_wifi__scan__results__available =
{
  {
    -1,
    (gchar *) "WIFI_SCAN_RESULTS_AVAILABLE",
    NULL,
    NULL
  },
  "wifi--scan--results--available"
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed_ARG_currentState =
{
  {
    -1,
    (gchar *) "currentState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed_ARG_previousState =
{
  {
    -1,
    (gchar *) "previousState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_signal_info_wifi__state__changed_ARG_pointers[] =
{
  &_wifi_service_signal_info_wifi__state__changed_ARG_currentState.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed_ARG_previousState.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wifi_service_signal_info_wifi__state__changed =
{
  {
    -1,
    (gchar *) "WIFI_STATE_CHANGED",
    (GDBusArgInfo **) &_wifi_service_signal_info_wifi__state__changed_ARG_pointers,
    NULL
  },
  "wifi--state--changed"
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed__host_ARG_currentState =
{
  {
    -1,
    (gchar *) "currentState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed__host_ARG_previousState =
{
  {
    -1,
    (gchar *) "previousState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_signal_info_wifi__state__changed__host_ARG_pointers[] =
{
  &_wifi_service_signal_info_wifi__state__changed__host_ARG_currentState.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed__host_ARG_previousState.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wifi_service_signal_info_wifi__state__changed__host =
{
  {
    -1,
    (gchar *) "WIFI_STATE_CHANGED_HOST",
    (GDBusArgInfo **) &_wifi_service_signal_info_wifi__state__changed__host_ARG_pointers,
    NULL
  },
  "wifi--state--changed--host"
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed__hoatap_ARG_currentState =
{
  {
    -1,
    (gchar *) "currentState",
    (gchar *) "(is)",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__state__changed__hoatap_ARG_previousState =
{
  {
    -1,
    (gchar *) "previousState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_signal_info_wifi__state__changed__hoatap_ARG_pointers[] =
{
  &_wifi_service_signal_info_wifi__state__changed__hoatap_ARG_currentState.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed__hoatap_ARG_previousState.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wifi_service_signal_info_wifi__state__changed__hoatap =
{
  {
    -1,
    (gchar *) "WIFI_STATE_CHANGED_HOATAP",
    (GDBusArgInfo **) &_wifi_service_signal_info_wifi__state__changed__hoatap_ARG_pointers,
    NULL
  },
  "wifi--state--changed--hoatap"
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__signal__level__changed_ARG_currentLevel =
{
  {
    -1,
    (gchar *) "currentLevel",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _wifi_service_signal_info_wifi__signal__level__changed_ARG_previousLevel =
{
  {
    -1,
    (gchar *) "previousLevel",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _wifi_service_signal_info_wifi__signal__level__changed_ARG_pointers[] =
{
  &_wifi_service_signal_info_wifi__signal__level__changed_ARG_currentLevel.parent_struct,
  &_wifi_service_signal_info_wifi__signal__level__changed_ARG_previousLevel.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _wifi_service_signal_info_wifi__signal__level__changed =
{
  {
    -1,
    (gchar *) "WIFI_SIGNAL_LEVEL_CHANGED",
    (GDBusArgInfo **) &_wifi_service_signal_info_wifi__signal__level__changed_ARG_pointers,
    NULL
  },
  "wifi--signal--level--changed"
};

static const GDBusSignalInfo * const _wifi_service_signal_info_pointers[] =
{
  &_wifi_service_signal_info_wifi__scan__results__available.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed__host.parent_struct,
  &_wifi_service_signal_info_wifi__state__changed__hoatap.parent_struct,
  &_wifi_service_signal_info_wifi__signal__level__changed.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _wifi_service_property_info_periodic_scan_interval =
{
  {
    -1,
    (gchar *) "PeriodicScanInterval",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "periodic-scan-interval",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _wifi_service_property_info_state =
{
  {
    -1,
    (gchar *) "State",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "state",
  FALSE
};

static const GDBusPropertyInfo * const _wifi_service_property_info_pointers[] =
{
  &_wifi_service_property_info_periodic_scan_interval.parent_struct,
  &_wifi_service_property_info_state.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _wifi_service_interface_info =
{
  {
    -1,
    (gchar *) "com.hsaeyz.sal.WifiService",
    (GDBusMethodInfo **) &_wifi_service_method_info_pointers,
    (GDBusSignalInfo **) &_wifi_service_signal_info_pointers,
    (GDBusPropertyInfo **) &_wifi_service_property_info_pointers,
    NULL
  },
  "wifi-service",
};


/**
 * wifi_service_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
wifi_service_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct;
}

/**
 * wifi_service_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #WifiService interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
wifi_service_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "periodic-scan-interval");
  g_object_class_override_property (klass, property_id_begin++, "state");
  return property_id_begin - 1;
}



/**
 * WifiService:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link>.
 */

/**
 * WifiServiceIface:
 * @parent_iface: The parent interface.
 * @handle_addsta: Handler for the #WifiService::handle-addsta signal.
 * @handle_connect: Handler for the #WifiService::handle-connect signal.
 * @handle_deletesta: Handler for the #WifiService::handle-deletesta signal.
 * @handle_disconnect: Handler for the #WifiService::handle-disconnect signal.
 * @handle_get_connection_info: Handler for the #WifiService::handle-get-connection-info signal.
 * @handle_get_host_ap_client_info: Handler for the #WifiService::handle-get-host-ap-client-info signal.
 * @handle_get_saved_networks: Handler for the #WifiService::handle-get-saved-networks signal.
 * @handle_get_scan_results: Handler for the #WifiService::handle-get-scan-results signal.
 * @handle_is_wifi_enabled: Handler for the #WifiService::handle-is-wifi-enabled signal.
 * @handle_select_network: Handler for the #WifiService::handle-select-network signal.
 * @handle_set_periodic_scan_interval: Handler for the #WifiService::handle-set-periodic-scan-interval signal.
 * @handle_set_wifi_enabled: Handler for the #WifiService::handle-set-wifi-enabled signal.
 * @handle_start_host_ap: Handler for the #WifiService::handle-start-host-ap signal.
 * @handle_start_scan: Handler for the #WifiService::handle-start-scan signal.
 * @handle_stop_host_ap: Handler for the #WifiService::handle-stop-host-ap signal.
 * @handle_update_host_ap_mac: Handler for the #WifiService::handle-update-host-ap-mac signal.
 * @get_periodic_scan_interval: Getter for the #WifiService:periodic-scan-interval property.
 * @get_state: Getter for the #WifiService:state property.
 * @wifi__scan__results__available: Handler for the #WifiService::wifi--scan--results--available signal.
 * @wifi__signal__level__changed: Handler for the #WifiService::wifi--signal--level--changed signal.
 * @wifi__state__changed: Handler for the #WifiService::wifi--state--changed signal.
 * @wifi__state__changed__hoatap: Handler for the #WifiService::wifi--state--changed--hoatap signal.
 * @wifi__state__changed__host: Handler for the #WifiService::wifi--state--changed--host signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link>.
 */

typedef WifiServiceIface WifiServiceInterface;
G_DEFINE_INTERFACE (WifiService, wifi_service, G_TYPE_OBJECT)

static void
wifi_service_default_init (WifiServiceIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * WifiService::handle-set-wifi-enabled:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_enabled: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setWifiEnabled">setWifiEnabled()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_set_wifi_enabled() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-wifi-enabled",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_set_wifi_enabled),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-start-scan:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startScan">startScan()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_start_scan() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-scan",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_start_scan),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-get-scan-results:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getScanResults">getScanResults()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_get_scan_results() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-scan-results",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_get_scan_results),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-connect:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.connect">connect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_connect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_connect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-update-host-ap-mac:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.updateHostApMac">updateHostApMac()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_update_host_ap_mac() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-update-host-ap-mac",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_update_host_ap_mac),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-addsta:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.addsta">addsta()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_addsta() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-addsta",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_addsta),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-deletesta:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.deletesta">deletesta()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_deletesta() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-deletesta",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_deletesta),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-start-host-ap:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startHostAp">startHostAp()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_start_host_ap() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-host-ap",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_start_host_ap),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-stop-host-ap:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_config: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.stopHostAp">stopHostAp()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_stop_host_ap() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-host-ap",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_stop_host_ap),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-get-host-ap-client-info:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getHostApClientInfo">getHostApClientInfo()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_get_host_ap_client_info() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-host-ap-client-info",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_get_host_ap_client_info),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-disconnect:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.disconnect">disconnect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_disconnect() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-disconnect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_disconnect),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-set-periodic-scan-interval:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interval: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setPeriodicScanInterval">setPeriodicScanInterval()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_set_periodic_scan_interval() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-periodic-scan-interval",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_set_periodic_scan_interval),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /**
   * WifiService::handle-is-wifi-enabled:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.isWifiEnabled">isWifiEnabled()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_is_wifi_enabled() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-is-wifi-enabled",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_is_wifi_enabled),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-get-connection-info:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getConnectionInfo">getConnectionInfo()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_get_connection_info() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-connection-info",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_get_connection_info),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-get-saved-networks:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getSavedNetworks">getSavedNetworks()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_get_saved_networks() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-saved-networks",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_get_saved_networks),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * WifiService::handle-select-network:
   * @object: A #WifiService.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_netid: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.selectNetwork">selectNetwork()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call wifi_service_complete_select_network() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-select-network",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, handle_select_network),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_VARIANT);

  /* GObject signals for received D-Bus signals: */
  /**
   * WifiService::wifi--scan--results--available:
   * @object: A #WifiService.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_SCAN_RESULTS_AVAILABLE">"WIFI_SCAN_RESULTS_AVAILABLE"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("wifi--scan--results--available",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, wifi__scan__results__available),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    0);

  /**
   * WifiService::wifi--state--changed:
   * @object: A #WifiService.
   * @arg_currentState: Argument.
   * @arg_previousState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED">"WIFI_STATE_CHANGED"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("wifi--state--changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, wifi__state__changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_INT);

  /**
   * WifiService::wifi--state--changed--host:
   * @object: A #WifiService.
   * @arg_currentState: Argument.
   * @arg_previousState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED_HOST">"WIFI_STATE_CHANGED_HOST"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("wifi--state--changed--host",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, wifi__state__changed__host),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_INT);

  /**
   * WifiService::wifi--state--changed--hoatap:
   * @object: A #WifiService.
   * @arg_currentState: Argument.
   * @arg_previousState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED_HOATAP">"WIFI_STATE_CHANGED_HOATAP"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("wifi--state--changed--hoatap",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, wifi__state__changed__hoatap),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_VARIANT, G_TYPE_INT);

  /**
   * WifiService::wifi--signal--level--changed:
   * @object: A #WifiService.
   * @arg_currentLevel: Argument.
   * @arg_previousLevel: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_SIGNAL_LEVEL_CHANGED">"WIFI_SIGNAL_LEVEL_CHANGED"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("wifi--signal--level--changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (WifiServiceIface, wifi__signal__level__changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_INT, G_TYPE_INT);

  /* GObject properties for D-Bus properties: */
  /**
   * WifiService:periodic-scan-interval:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.PeriodicScanInterval">"PeriodicScanInterval"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("periodic-scan-interval", "PeriodicScanInterval", "PeriodicScanInterval", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * WifiService:state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.State">"State"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("state", "State", "State", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * wifi_service_get_periodic_scan_interval: (skip)
 * @object: A #WifiService.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.PeriodicScanInterval">"PeriodicScanInterval"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
wifi_service_get_periodic_scan_interval (WifiService *object)
{
  return WIFI_SERVICE_GET_IFACE (object)->get_periodic_scan_interval (object);
}

/**
 * wifi_service_set_periodic_scan_interval: (skip)
 * @object: A #WifiService.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.PeriodicScanInterval">"PeriodicScanInterval"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
wifi_service_set_periodic_scan_interval (WifiService *object, gint value)
{
  g_object_set (G_OBJECT (object), "periodic-scan-interval", value, NULL);
}

/**
 * wifi_service_get_state: (skip)
 * @object: A #WifiService.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.State">"State"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
wifi_service_get_state (WifiService *object)
{
  return WIFI_SERVICE_GET_IFACE (object)->get_state (object);
}

/**
 * wifi_service_set_state: (skip)
 * @object: A #WifiService.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-sal-WifiService.State">"State"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
wifi_service_set_state (WifiService *object, gint value)
{
  g_object_set (G_OBJECT (object), "state", value, NULL);
}

/**
 * wifi_service_emit_wifi__scan__results__available:
 * @object: A #WifiService.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_SCAN_RESULTS_AVAILABLE">"WIFI_SCAN_RESULTS_AVAILABLE"</link> D-Bus signal.
 */
void
wifi_service_emit_wifi__scan__results__available (
    WifiService *object)
{
  g_signal_emit_by_name (object, "wifi--scan--results--available");
}

/**
 * wifi_service_emit_wifi__state__changed:
 * @object: A #WifiService.
 * @arg_currentState: Argument to pass with the signal.
 * @arg_previousState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED">"WIFI_STATE_CHANGED"</link> D-Bus signal.
 */
void
wifi_service_emit_wifi__state__changed (
    WifiService *object,
    gint arg_currentState,
    gint arg_previousState)
{
  g_signal_emit_by_name (object, "wifi--state--changed", arg_currentState, arg_previousState);
}

/**
 * wifi_service_emit_wifi__state__changed__host:
 * @object: A #WifiService.
 * @arg_currentState: Argument to pass with the signal.
 * @arg_previousState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED_HOST">"WIFI_STATE_CHANGED_HOST"</link> D-Bus signal.
 */
void
wifi_service_emit_wifi__state__changed__host (
    WifiService *object,
    gint arg_currentState,
    gint arg_previousState)
{
  g_signal_emit_by_name (object, "wifi--state--changed--host", arg_currentState, arg_previousState);
}

/**
 * wifi_service_emit_wifi__state__changed__hoatap:
 * @object: A #WifiService.
 * @arg_currentState: Argument to pass with the signal.
 * @arg_previousState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_STATE_CHANGED_HOATAP">"WIFI_STATE_CHANGED_HOATAP"</link> D-Bus signal.
 */
void
wifi_service_emit_wifi__state__changed__hoatap (
    WifiService *object,
    GVariant *arg_currentState,
    gint arg_previousState)
{
  g_signal_emit_by_name (object, "wifi--state--changed--hoatap", arg_currentState, arg_previousState);
}

/**
 * wifi_service_emit_wifi__signal__level__changed:
 * @object: A #WifiService.
 * @arg_currentLevel: Argument to pass with the signal.
 * @arg_previousLevel: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-sal-WifiService.WIFI_SIGNAL_LEVEL_CHANGED">"WIFI_SIGNAL_LEVEL_CHANGED"</link> D-Bus signal.
 */
void
wifi_service_emit_wifi__signal__level__changed (
    WifiService *object,
    gint arg_currentLevel,
    gint arg_previousLevel)
{
  g_signal_emit_by_name (object, "wifi--signal--level--changed", arg_currentLevel, arg_previousLevel);
}

/**
 * wifi_service_call_set_wifi_enabled:
 * @proxy: A #WifiServiceProxy.
 * @arg_enabled: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setWifiEnabled">setWifiEnabled()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_set_wifi_enabled_finish() to get the result of the operation.
 *
 * See wifi_service_call_set_wifi_enabled_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_set_wifi_enabled (
    WifiService *proxy,
    GVariant *arg_enabled,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setWifiEnabled",
    g_variant_new ("(@(b))",
                   arg_enabled),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_set_wifi_enabled_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_set_wifi_enabled().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_set_wifi_enabled().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_set_wifi_enabled_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_set_wifi_enabled_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_enabled: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setWifiEnabled">setWifiEnabled()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_set_wifi_enabled() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_set_wifi_enabled_sync (
    WifiService *proxy,
    GVariant *arg_enabled,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setWifiEnabled",
    g_variant_new ("(@(b))",
                   arg_enabled),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_start_scan:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startScan">startScan()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_start_scan_finish() to get the result of the operation.
 *
 * See wifi_service_call_start_scan_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_start_scan (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "startScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_start_scan_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_start_scan().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_start_scan().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_start_scan_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_start_scan_sync:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startScan">startScan()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_start_scan() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_start_scan_sync (
    WifiService *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "startScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_scan_results:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getScanResults">getScanResults()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_get_scan_results_finish() to get the result of the operation.
 *
 * See wifi_service_call_get_scan_results_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_get_scan_results (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getScanResults",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_get_scan_results_finish:
 * @proxy: A #WifiServiceProxy.
 * @out_results: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_get_scan_results().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_get_scan_results().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_scan_results_finish (
    WifiService *proxy,
    GVariant **out_results,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssiiiasb))",
                 out_results);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_scan_results_sync:
 * @proxy: A #WifiServiceProxy.
 * @out_results: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getScanResults">getScanResults()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_get_scan_results() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_scan_results_sync (
    WifiService *proxy,
    GVariant **out_results,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getScanResults",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(ssiiiasb))",
                 out_results);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_connect:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.connect">connect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_connect_finish() to get the result of the operation.
 *
 * See wifi_service_call_connect_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_connect (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "connect",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_connect_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_connect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_connect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_connect_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_connect_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.connect">connect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_connect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_connect_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "connect",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_update_host_ap_mac:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.updateHostApMac">updateHostApMac()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_update_host_ap_mac_finish() to get the result of the operation.
 *
 * See wifi_service_call_update_host_ap_mac_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_update_host_ap_mac (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "updateHostApMac",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_update_host_ap_mac_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_update_host_ap_mac().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_update_host_ap_mac().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_update_host_ap_mac_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_update_host_ap_mac_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.updateHostApMac">updateHostApMac()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_update_host_ap_mac() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_update_host_ap_mac_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "updateHostApMac",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_addsta:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.addsta">addsta()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_addsta_finish() to get the result of the operation.
 *
 * See wifi_service_call_addsta_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_addsta (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "addsta",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_addsta_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_addsta().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_addsta().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_addsta_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_addsta_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.addsta">addsta()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_addsta() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_addsta_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "addsta",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_deletesta:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.deletesta">deletesta()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_deletesta_finish() to get the result of the operation.
 *
 * See wifi_service_call_deletesta_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_deletesta (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "deletesta",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_deletesta_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_deletesta().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_deletesta().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_deletesta_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_deletesta_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.deletesta">deletesta()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_deletesta() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_deletesta_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "deletesta",
    g_variant_new ("(@(s))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_start_host_ap:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startHostAp">startHostAp()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_start_host_ap_finish() to get the result of the operation.
 *
 * See wifi_service_call_start_host_ap_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_start_host_ap (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "startHostAp",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_start_host_ap_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_start_host_ap().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_start_host_ap().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_start_host_ap_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_start_host_ap_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startHostAp">startHostAp()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_start_host_ap() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_start_host_ap_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "startHostAp",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_stop_host_ap:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.stopHostAp">stopHostAp()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_stop_host_ap_finish() to get the result of the operation.
 *
 * See wifi_service_call_stop_host_ap_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_stop_host_ap (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stopHostAp",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_stop_host_ap_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_stop_host_ap().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_stop_host_ap().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_stop_host_ap_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_stop_host_ap_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_config: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.stopHostAp">stopHostAp()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_stop_host_ap() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_stop_host_ap_sync (
    WifiService *proxy,
    GVariant *arg_config,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stopHostAp",
    g_variant_new ("(@(ss))",
                   arg_config),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_host_ap_client_info:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getHostApClientInfo">getHostApClientInfo()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_get_host_ap_client_info_finish() to get the result of the operation.
 *
 * See wifi_service_call_get_host_ap_client_info_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_get_host_ap_client_info (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getHostApClientInfo",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_get_host_ap_client_info_finish:
 * @proxy: A #WifiServiceProxy.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_get_host_ap_client_info().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_get_host_ap_client_info().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_host_ap_client_info_finish (
    WifiService *proxy,
    GVariant **out_info,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(ssiisiiiss))",
                 out_info);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_host_ap_client_info_sync:
 * @proxy: A #WifiServiceProxy.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getHostApClientInfo">getHostApClientInfo()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_get_host_ap_client_info() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_host_ap_client_info_sync (
    WifiService *proxy,
    GVariant **out_info,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getHostApClientInfo",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(ssiisiiiss))",
                 out_info);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_disconnect:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.disconnect">disconnect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_disconnect_finish() to get the result of the operation.
 *
 * See wifi_service_call_disconnect_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_disconnect (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_disconnect_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_disconnect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_disconnect().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_disconnect_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_disconnect_sync:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.disconnect">disconnect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_disconnect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_disconnect_sync (
    WifiService *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "disconnect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_set_periodic_scan_interval:
 * @proxy: A #WifiServiceProxy.
 * @arg_interval: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setPeriodicScanInterval">setPeriodicScanInterval()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_set_periodic_scan_interval_finish() to get the result of the operation.
 *
 * See wifi_service_call_set_periodic_scan_interval_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_set_periodic_scan_interval (
    WifiService *proxy,
    GVariant *arg_interval,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "setPeriodicScanInterval",
    g_variant_new ("(@(i))",
                   arg_interval),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_set_periodic_scan_interval_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_set_periodic_scan_interval().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_set_periodic_scan_interval().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_set_periodic_scan_interval_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_set_periodic_scan_interval_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_interval: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setPeriodicScanInterval">setPeriodicScanInterval()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_set_periodic_scan_interval() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_set_periodic_scan_interval_sync (
    WifiService *proxy,
    GVariant *arg_interval,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "setPeriodicScanInterval",
    g_variant_new ("(@(i))",
                   arg_interval),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_is_wifi_enabled:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.isWifiEnabled">isWifiEnabled()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_is_wifi_enabled_finish() to get the result of the operation.
 *
 * See wifi_service_call_is_wifi_enabled_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_is_wifi_enabled (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "isWifiEnabled",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_is_wifi_enabled_finish:
 * @proxy: A #WifiServiceProxy.
 * @out_enabled: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_is_wifi_enabled().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_is_wifi_enabled().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_is_wifi_enabled_finish (
    WifiService *proxy,
    gboolean *out_enabled,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_enabled);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_is_wifi_enabled_sync:
 * @proxy: A #WifiServiceProxy.
 * @out_enabled: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.isWifiEnabled">isWifiEnabled()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_is_wifi_enabled() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_is_wifi_enabled_sync (
    WifiService *proxy,
    gboolean *out_enabled,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "isWifiEnabled",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(b)",
                 out_enabled);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_connection_info:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getConnectionInfo">getConnectionInfo()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_get_connection_info_finish() to get the result of the operation.
 *
 * See wifi_service_call_get_connection_info_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_get_connection_info (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getConnectionInfo",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_get_connection_info_finish:
 * @proxy: A #WifiServiceProxy.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_get_connection_info().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_get_connection_info().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_connection_info_finish (
    WifiService *proxy,
    GVariant **out_info,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(ssiisiiiss))",
                 out_info);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_connection_info_sync:
 * @proxy: A #WifiServiceProxy.
 * @out_info: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getConnectionInfo">getConnectionInfo()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_get_connection_info() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_connection_info_sync (
    WifiService *proxy,
    GVariant **out_info,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getConnectionInfo",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@(ssiisiiiss))",
                 out_info);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_saved_networks:
 * @proxy: A #WifiServiceProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getSavedNetworks">getSavedNetworks()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_get_saved_networks_finish() to get the result of the operation.
 *
 * See wifi_service_call_get_saved_networks_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_get_saved_networks (
    WifiService *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getSavedNetworks",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_get_saved_networks_finish:
 * @proxy: A #WifiServiceProxy.
 * @out_networks: (out): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_get_saved_networks().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_get_saved_networks().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_saved_networks_finish (
    WifiService *proxy,
    GVariant **out_networks,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(issibb))",
                 out_networks);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_get_saved_networks_sync:
 * @proxy: A #WifiServiceProxy.
 * @out_networks: (out): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getSavedNetworks">getSavedNetworks()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_get_saved_networks() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_get_saved_networks_sync (
    WifiService *proxy,
    GVariant **out_networks,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getSavedNetworks",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a(issibb))",
                 out_networks);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_select_network:
 * @proxy: A #WifiServiceProxy.
 * @arg_netid: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.selectNetwork">selectNetwork()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_call_select_network_finish() to get the result of the operation.
 *
 * See wifi_service_call_select_network_sync() for the synchronous, blocking version of this method.
 */
void
wifi_service_call_select_network (
    WifiService *proxy,
    GVariant *arg_netid,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "selectNetwork",
    g_variant_new ("(@(i))",
                   arg_netid),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * wifi_service_call_select_network_finish:
 * @proxy: A #WifiServiceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_call_select_network().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with wifi_service_call_select_network().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_select_network_finish (
    WifiService *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_call_select_network_sync:
 * @proxy: A #WifiServiceProxy.
 * @arg_netid: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.selectNetwork">selectNetwork()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See wifi_service_call_select_network() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
wifi_service_call_select_network_sync (
    WifiService *proxy,
    GVariant *arg_netid,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "selectNetwork",
    g_variant_new ("(@(i))",
                   arg_netid),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * wifi_service_complete_set_wifi_enabled:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setWifiEnabled">setWifiEnabled()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_set_wifi_enabled (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_start_scan:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startScan">startScan()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_start_scan (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_get_scan_results:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @results: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getScanResults">getScanResults()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_get_scan_results (
    WifiService *object,
    GDBusMethodInvocation *invocation,
    GVariant *results)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(ssiiiasb))",
                   results));
}

/**
 * wifi_service_complete_connect:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.connect">connect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_connect (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_update_host_ap_mac:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.updateHostApMac">updateHostApMac()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_update_host_ap_mac (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_addsta:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.addsta">addsta()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_addsta (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_deletesta:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.deletesta">deletesta()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_deletesta (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_start_host_ap:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.startHostAp">startHostAp()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_start_host_ap (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_stop_host_ap:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.stopHostAp">stopHostAp()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_stop_host_ap (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_get_host_ap_client_info:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @info: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getHostApClientInfo">getHostApClientInfo()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_get_host_ap_client_info (
    WifiService *object,
    GDBusMethodInvocation *invocation,
    GVariant *info)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(ssiisiiiss))",
                   info));
}

/**
 * wifi_service_complete_disconnect:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.disconnect">disconnect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_disconnect (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_set_periodic_scan_interval:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.setPeriodicScanInterval">setPeriodicScanInterval()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_set_periodic_scan_interval (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * wifi_service_complete_is_wifi_enabled:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @enabled: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.isWifiEnabled">isWifiEnabled()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_is_wifi_enabled (
    WifiService *object,
    GDBusMethodInvocation *invocation,
    gboolean enabled)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(b)",
                   enabled));
}

/**
 * wifi_service_complete_get_connection_info:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @info: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getConnectionInfo">getConnectionInfo()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_get_connection_info (
    WifiService *object,
    GDBusMethodInvocation *invocation,
    GVariant *info)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@(ssiisiiiss))",
                   info));
}

/**
 * wifi_service_complete_get_saved_networks:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @networks: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.getSavedNetworks">getSavedNetworks()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_get_saved_networks (
    WifiService *object,
    GDBusMethodInvocation *invocation,
    GVariant *networks)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a(issibb))",
                   networks));
}

/**
 * wifi_service_complete_select_network:
 * @object: A #WifiService.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-sal-WifiService.selectNetwork">selectNetwork()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
wifi_service_complete_select_network (
    WifiService *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * WifiServiceProxy:
 *
 * The #WifiServiceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * WifiServiceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #WifiServiceProxy.
 */

struct _WifiServiceProxyPrivate
{
  GData *qdata;
};

static void wifi_service_proxy_iface_init (WifiServiceIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (WifiServiceProxy, wifi_service_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (WifiServiceProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_WIFI_SERVICE, wifi_service_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (WifiServiceProxy, wifi_service_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_WIFI_SERVICE, wifi_service_proxy_iface_init))

#endif
static void
wifi_service_proxy_finalize (GObject *object)
{
  WifiServiceProxy *proxy = WIFI_SERVICE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (wifi_service_proxy_parent_class)->finalize (object);
}

static void
wifi_service_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _wifi_service_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
wifi_service_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.hsaeyz.sal.WifiService: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
wifi_service_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = (const _ExtendedGDBusPropertyInfo *) _wifi_service_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.hsaeyz.sal.WifiService", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) wifi_service_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
wifi_service_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_WIFI_SERVICE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_WIFI_SERVICE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
wifi_service_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  WifiServiceProxy *proxy = WIFI_SERVICE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gint 
wifi_service_proxy_get_periodic_scan_interval (WifiService *object)
{
  WifiServiceProxy *proxy = WIFI_SERVICE_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PeriodicScanInterval");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
wifi_service_proxy_get_state (WifiService *object)
{
  WifiServiceProxy *proxy = WIFI_SERVICE_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "State");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
wifi_service_proxy_init (WifiServiceProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = wifi_service_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_WIFI_SERVICE_PROXY, WifiServiceProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), wifi_service_interface_info ());
}

static void
wifi_service_proxy_class_init (WifiServiceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = wifi_service_proxy_finalize;
  gobject_class->get_property = wifi_service_proxy_get_property;
  gobject_class->set_property = wifi_service_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = wifi_service_proxy_g_signal;
  proxy_class->g_properties_changed = wifi_service_proxy_g_properties_changed;

  wifi_service_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (WifiServiceProxyPrivate));
#endif
}

static void
wifi_service_proxy_iface_init (WifiServiceIface *iface)
{
  iface->get_periodic_scan_interval = wifi_service_proxy_get_periodic_scan_interval;
  iface->get_state = wifi_service_proxy_get_state;
}

/**
 * wifi_service_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_proxy_new_finish() to get the result of the operation.
 *
 * See wifi_service_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
wifi_service_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_WIFI_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.sal.WifiService", NULL);
}

/**
 * wifi_service_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with wifi_service_proxy_new().
 *
 * Returns: (transfer full) (type WifiServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
WifiService *
wifi_service_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return WIFI_SERVICE (ret);
  else
    return NULL;
}

/**
 * wifi_service_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See wifi_service_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type WifiServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
WifiService *
wifi_service_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_WIFI_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.sal.WifiService", NULL);
  if (ret != NULL)
    return WIFI_SERVICE (ret);
  else
    return NULL;
}


/**
 * wifi_service_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like wifi_service_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call wifi_service_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See wifi_service_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
wifi_service_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_WIFI_SERVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.sal.WifiService", NULL);
}

/**
 * wifi_service_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to wifi_service_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with wifi_service_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type WifiServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
WifiService *
wifi_service_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return WIFI_SERVICE (ret);
  else
    return NULL;
}

/**
 * wifi_service_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like wifi_service_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See wifi_service_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type WifiServiceProxy): The constructed proxy object or %NULL if @error is set.
 */
WifiService *
wifi_service_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_WIFI_SERVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.sal.WifiService", NULL);
  if (ret != NULL)
    return WIFI_SERVICE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * WifiServiceSkeleton:
 *
 * The #WifiServiceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * WifiServiceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #WifiServiceSkeleton.
 */

struct _WifiServiceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_wifi_service_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_WIFI_SERVICE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_WIFI_SERVICE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_wifi_service_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_wifi_service_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_wifi_service_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _wifi_service_skeleton_vtable =
{
  _wifi_service_skeleton_handle_method_call,
  _wifi_service_skeleton_handle_get_property,
  _wifi_service_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
wifi_service_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return wifi_service_interface_info ();
}

static GDBusInterfaceVTable *
wifi_service_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_wifi_service_skeleton_vtable;
}

static GVariant *
wifi_service_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_wifi_service_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _wifi_service_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _wifi_service_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _wifi_service_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _wifi_service_emit_changed (gpointer user_data);

static void
wifi_service_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _wifi_service_emit_changed (skeleton);
}

static void
_wifi_service_on_signal_wifi__scan__results__available (
    WifiService *object)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("()"));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", "WIFI_SCAN_RESULTS_AVAILABLE",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wifi_service_on_signal_wifi__state__changed (
    WifiService *object,
    gint arg_currentState,
    gint arg_previousState)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ii)",
                   arg_currentState,
                   arg_previousState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", "WIFI_STATE_CHANGED",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wifi_service_on_signal_wifi__state__changed__host (
    WifiService *object,
    gint arg_currentState,
    gint arg_previousState)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ii)",
                   arg_currentState,
                   arg_previousState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", "WIFI_STATE_CHANGED_HOST",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wifi_service_on_signal_wifi__state__changed__hoatap (
    WifiService *object,
    GVariant *arg_currentState,
    gint arg_previousState)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(@(is)i)",
                   arg_currentState,
                   arg_previousState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", "WIFI_STATE_CHANGED_HOATAP",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_wifi_service_on_signal_wifi__signal__level__changed (
    WifiService *object,
    gint arg_currentLevel,
    gint arg_previousLevel)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ii)",
                   arg_currentLevel,
                   arg_previousLevel));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.sal.WifiService", "WIFI_SIGNAL_LEVEL_CHANGED",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void wifi_service_skeleton_iface_init (WifiServiceIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (WifiServiceSkeleton, wifi_service_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (WifiServiceSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_WIFI_SERVICE, wifi_service_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (WifiServiceSkeleton, wifi_service_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_WIFI_SERVICE, wifi_service_skeleton_iface_init))

#endif
static void
wifi_service_skeleton_finalize (GObject *object)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (wifi_service_skeleton_parent_class)->finalize (object);
}

static void
wifi_service_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_wifi_service_emit_changed (gpointer user_data)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.hsaeyz.sal.WifiService",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_wifi_service_schedule_emit_changed (WifiServiceSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
wifi_service_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _wifi_service_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _wifi_service_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
wifi_service_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _wifi_service_schedule_emit_changed (skeleton, (const _ExtendedGDBusPropertyInfo *) _wifi_service_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
wifi_service_skeleton_init (WifiServiceSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = wifi_service_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_WIFI_SERVICE_SKELETON, WifiServiceSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_INT);
}

static gint 
wifi_service_skeleton_get_periodic_scan_interval (WifiService *object)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
wifi_service_skeleton_get_state (WifiService *object)
{
  WifiServiceSkeleton *skeleton = WIFI_SERVICE_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
wifi_service_skeleton_class_init (WifiServiceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = wifi_service_skeleton_finalize;
  gobject_class->get_property = wifi_service_skeleton_get_property;
  gobject_class->set_property = wifi_service_skeleton_set_property;
  gobject_class->notify       = wifi_service_skeleton_notify;


  wifi_service_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = wifi_service_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = wifi_service_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = wifi_service_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = wifi_service_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (WifiServiceSkeletonPrivate));
#endif
}

static void
wifi_service_skeleton_iface_init (WifiServiceIface *iface)
{
  iface->wifi__scan__results__available = _wifi_service_on_signal_wifi__scan__results__available;
  iface->wifi__state__changed = _wifi_service_on_signal_wifi__state__changed;
  iface->wifi__state__changed__host = _wifi_service_on_signal_wifi__state__changed__host;
  iface->wifi__state__changed__hoatap = _wifi_service_on_signal_wifi__state__changed__hoatap;
  iface->wifi__signal__level__changed = _wifi_service_on_signal_wifi__signal__level__changed;
  iface->get_periodic_scan_interval = wifi_service_skeleton_get_periodic_scan_interval;
  iface->get_state = wifi_service_skeleton_get_state;
}

/**
 * wifi_service_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-sal-WifiService.top_of_page">com.hsaeyz.sal.WifiService</link>.
 *
 * Returns: (transfer full) (type WifiServiceSkeleton): The skeleton object.
 */
WifiService *
wifi_service_skeleton_new (void)
{
  return WIFI_SERVICE (g_object_new (TYPE_WIFI_SERVICE_SKELETON, NULL));
}

