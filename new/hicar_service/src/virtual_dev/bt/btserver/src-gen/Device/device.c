/*
 * Generated by gdbus-codegen 2.40.2. DO NOT EDIT.
 *
 * The license of this code is the same as for the source it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "device.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface com.hsaeyz.bluetooth.device
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:Device
 * @title: Device
 * @short_description: Generated C code for the com.hsaeyz.bluetooth.device D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link> D-Bus interface in C.
 */

/* ---- Introspection data for com.hsaeyz.bluetooth.device ---- */

static const _ExtendedGDBusMethodInfo _device_method_info_start_scan =
{
  {
    -1,
    (gchar *) "startScan",
    NULL,
    NULL,
    NULL
  },
  "handle-start-scan",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_stop_scan =
{
  {
    -1,
    (gchar *) "stopScan",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-scan",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_accpect_pair =
{
  {
    -1,
    (gchar *) "accpectPair",
    NULL,
    NULL,
    NULL
  },
  "handle-accpect-pair",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_refuse_pair =
{
  {
    -1,
    (gchar *) "refusePair",
    NULL,
    NULL,
    NULL
  },
  "handle-refuse-pair",
  FALSE
};

static const _ExtendedGDBusArgInfo _device_method_info_delete_pair_device_IN_ARG_btAddress =
{
  {
    -1,
    (gchar *) "btAddress",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_method_info_delete_pair_device_IN_ARG_pointers[] =
{
  &_device_method_info_delete_pair_device_IN_ARG_btAddress,
  NULL
};

static const _ExtendedGDBusMethodInfo _device_method_info_delete_pair_device =
{
  {
    -1,
    (gchar *) "deletePairDevice",
    (GDBusArgInfo **) &_device_method_info_delete_pair_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-delete-pair-device",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_clear_pair_device =
{
  {
    -1,
    (gchar *) "clearPairDevice",
    NULL,
    NULL,
    NULL
  },
  "handle-clear-pair-device",
  FALSE
};

static const _ExtendedGDBusArgInfo _device_method_info_connect_device_IN_ARG_btAddress =
{
  {
    -1,
    (gchar *) "btAddress",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_method_info_connect_device_IN_ARG_profileId =
{
  {
    -1,
    (gchar *) "profileId",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_method_info_connect_device_IN_ARG_pointers[] =
{
  &_device_method_info_connect_device_IN_ARG_btAddress,
  &_device_method_info_connect_device_IN_ARG_profileId,
  NULL
};

static const _ExtendedGDBusMethodInfo _device_method_info_connect_device =
{
  {
    -1,
    (gchar *) "connectDevice",
    (GDBusArgInfo **) &_device_method_info_connect_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-connect-device",
  FALSE
};

static const _ExtendedGDBusArgInfo _device_method_info_dis_connect_device_IN_ARG_btAddress =
{
  {
    -1,
    (gchar *) "btAddress",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_method_info_dis_connect_device_IN_ARG_profileId =
{
  {
    -1,
    (gchar *) "profileId",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_method_info_dis_connect_device_IN_ARG_pointers[] =
{
  &_device_method_info_dis_connect_device_IN_ARG_btAddress,
  &_device_method_info_dis_connect_device_IN_ARG_profileId,
  NULL
};

static const _ExtendedGDBusMethodInfo _device_method_info_dis_connect_device =
{
  {
    -1,
    (gchar *) "disConnectDevice",
    (GDBusArgInfo **) &_device_method_info_dis_connect_device_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-dis-connect-device",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_dis_connect_current_device =
{
  {
    -1,
    (gchar *) "disConnectCurrentDevice",
    NULL,
    NULL,
    NULL
  },
  "handle-dis-connect-current-device",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_get_paired_list =
{
  {
    -1,
    (gchar *) "getPairedList",
    NULL,
    NULL,
    NULL
  },
  "handle-get-paired-list",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_get_local_properties =
{
  {
    -1,
    (gchar *) "getLocalProperties",
    NULL,
    NULL,
    NULL
  },
  "handle-get-local-properties",
  FALSE
};

static const _ExtendedGDBusMethodInfo _device_method_info_get_current_states =
{
  {
    -1,
    (gchar *) "getCurrentStates",
    NULL,
    NULL,
    NULL
  },
  "handle-get-current-states",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _device_method_info_pointers[] =
{
  &_device_method_info_start_scan,
  &_device_method_info_stop_scan,
  &_device_method_info_accpect_pair,
  &_device_method_info_refuse_pair,
  &_device_method_info_delete_pair_device,
  &_device_method_info_clear_pair_device,
  &_device_method_info_connect_device,
  &_device_method_info_dis_connect_device,
  &_device_method_info_dis_connect_current_device,
  &_device_method_info_get_paired_list,
  &_device_method_info_get_local_properties,
  &_device_method_info_get_current_states,
  NULL
};

static const _ExtendedGDBusArgInfo _device_signal_info_pair_list_changed_ARG_pairList =
{
  {
    -1,
    (gchar *) "pairList",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_pair_list_changed_ARG_pointers[] =
{
  &_device_signal_info_pair_list_changed_ARG_pairList,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_pair_list_changed =
{
  {
    -1,
    (gchar *) "pairListChanged",
    (GDBusArgInfo **) &_device_signal_info_pair_list_changed_ARG_pointers,
    NULL
  },
  "pair-list-changed"
};

static const _ExtendedGDBusArgInfo _device_signal_info_requestpair_ARG_phonename =
{
  {
    -1,
    (gchar *) "phonename",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_requestpair_ARG_phoneaddr =
{
  {
    -1,
    (gchar *) "phoneaddr",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_requestpair_ARG_pointers[] =
{
  &_device_signal_info_requestpair_ARG_phonename,
  &_device_signal_info_requestpair_ARG_phoneaddr,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_requestpair =
{
  {
    -1,
    (gchar *) "requestpair",
    (GDBusArgInfo **) &_device_signal_info_requestpair_ARG_pointers,
    NULL
  },
  "requestpair"
};

static const _ExtendedGDBusArgInfo _device_signal_info_aclconnectstate_ARG_mac =
{
  {
    -1,
    (gchar *) "mac",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_aclconnectstate_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_aclconnectstate_ARG_pointers[] =
{
  &_device_signal_info_aclconnectstate_ARG_mac,
  &_device_signal_info_aclconnectstate_ARG_state,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_aclconnectstate =
{
  {
    -1,
    (gchar *) "aclconnectstate",
    (GDBusArgInfo **) &_device_signal_info_aclconnectstate_ARG_pointers,
    NULL
  },
  "aclconnectstate"
};

static const _ExtendedGDBusArgInfo _device_signal_info_disconnectreason_ARG_mac =
{
  {
    -1,
    (gchar *) "mac",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_disconnectreason_ARG_reason =
{
  {
    -1,
    (gchar *) "reason",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_disconnectreason_ARG_pointers[] =
{
  &_device_signal_info_disconnectreason_ARG_mac,
  &_device_signal_info_disconnectreason_ARG_reason,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_disconnectreason =
{
  {
    -1,
    (gchar *) "disconnectreason",
    (GDBusArgInfo **) &_device_signal_info_disconnectreason_ARG_pointers,
    NULL
  },
  "disconnectreason"
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_btAddress =
{
  {
    -1,
    (gchar *) "btAddress",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_hfpConnnectState =
{
  {
    -1,
    (gchar *) "hfpConnnectState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_pbapConnnectState =
{
  {
    -1,
    (gchar *) "pbapConnnectState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_avrcpConnnectState =
{
  {
    -1,
    (gchar *) "avrcpConnnectState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_a2cpConnnectState =
{
  {
    -1,
    (gchar *) "a2cpConnnectState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_callState =
{
  {
    -1,
    (gchar *) "callState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_phoneNumber =
{
  {
    -1,
    (gchar *) "phoneNumber",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_audioState =
{
  {
    -1,
    (gchar *) "audioState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_playState =
{
  {
    -1,
    (gchar *) "playState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_songLength =
{
  {
    -1,
    (gchar *) "songLength",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_current_state_changed_ARG_playPosition =
{
  {
    -1,
    (gchar *) "playPosition",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_current_state_changed_ARG_pointers[] =
{
  &_device_signal_info_current_state_changed_ARG_btAddress,
  &_device_signal_info_current_state_changed_ARG_hfpConnnectState,
  &_device_signal_info_current_state_changed_ARG_pbapConnnectState,
  &_device_signal_info_current_state_changed_ARG_avrcpConnnectState,
  &_device_signal_info_current_state_changed_ARG_a2cpConnnectState,
  &_device_signal_info_current_state_changed_ARG_callState,
  &_device_signal_info_current_state_changed_ARG_phoneNumber,
  &_device_signal_info_current_state_changed_ARG_audioState,
  &_device_signal_info_current_state_changed_ARG_playState,
  &_device_signal_info_current_state_changed_ARG_songLength,
  &_device_signal_info_current_state_changed_ARG_playPosition,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_current_state_changed =
{
  {
    -1,
    (gchar *) "currentStateChanged",
    (GDBusArgInfo **) &_device_signal_info_current_state_changed_ARG_pointers,
    NULL
  },
  "current-state-changed"
};

static const _ExtendedGDBusArgInfo _device_signal_info_connect_state_changed_ARG_address =
{
  {
    -1,
    (gchar *) "address",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_connect_state_changed_ARG_profileId =
{
  {
    -1,
    (gchar *) "profileId",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_connect_state_changed_ARG_connectState =
{
  {
    -1,
    (gchar *) "connectState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_connect_state_changed_ARG_pointers[] =
{
  &_device_signal_info_connect_state_changed_ARG_address,
  &_device_signal_info_connect_state_changed_ARG_profileId,
  &_device_signal_info_connect_state_changed_ARG_connectState,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_connect_state_changed =
{
  {
    -1,
    (gchar *) "connectStateChanged",
    (GDBusArgInfo **) &_device_signal_info_connect_state_changed_ARG_pointers,
    NULL
  },
  "connect-state-changed"
};

static const _ExtendedGDBusArgInfo _device_signal_info_local_property_changed_ARG_address =
{
  {
    -1,
    (gchar *) "address",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_local_property_changed_ARG_localName =
{
  {
    -1,
    (gchar *) "localName",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_local_property_changed_ARG_powerState =
{
  {
    -1,
    (gchar *) "powerState",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_local_property_changed_ARG_pointers[] =
{
  &_device_signal_info_local_property_changed_ARG_address,
  &_device_signal_info_local_property_changed_ARG_localName,
  &_device_signal_info_local_property_changed_ARG_powerState,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_local_property_changed =
{
  {
    -1,
    (gchar *) "localPropertyChanged",
    (GDBusArgInfo **) &_device_signal_info_local_property_changed_ARG_pointers,
    NULL
  },
  "local-property-changed"
};

static const _ExtendedGDBusArgInfo _device_signal_info_search_device_changed_ARG_address =
{
  {
    -1,
    (gchar *) "address",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _device_signal_info_search_device_changed_ARG_localName =
{
  {
    -1,
    (gchar *) "localName",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_search_device_changed_ARG_pointers[] =
{
  &_device_signal_info_search_device_changed_ARG_address,
  &_device_signal_info_search_device_changed_ARG_localName,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_search_device_changed =
{
  {
    -1,
    (gchar *) "searchDeviceChanged",
    (GDBusArgInfo **) &_device_signal_info_search_device_changed_ARG_pointers,
    NULL
  },
  "search-device-changed"
};

static const _ExtendedGDBusArgInfo _device_signal_info_battery_changed_ARG_batteryState =
{
  {
    -1,
    (gchar *) "batteryState",
    (gchar *) "i",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _device_signal_info_battery_changed_ARG_pointers[] =
{
  &_device_signal_info_battery_changed_ARG_batteryState,
  NULL
};

static const _ExtendedGDBusSignalInfo _device_signal_info_battery_changed =
{
  {
    -1,
    (gchar *) "batteryChanged",
    (GDBusArgInfo **) &_device_signal_info_battery_changed_ARG_pointers,
    NULL
  },
  "battery-changed"
};

static const _ExtendedGDBusSignalInfo * const _device_signal_info_pointers[] =
{
  &_device_signal_info_pair_list_changed,
  &_device_signal_info_requestpair,
  &_device_signal_info_aclconnectstate,
  &_device_signal_info_disconnectreason,
  &_device_signal_info_current_state_changed,
  &_device_signal_info_connect_state_changed,
  &_device_signal_info_local_property_changed,
  &_device_signal_info_search_device_changed,
  &_device_signal_info_battery_changed,
  NULL
};

static const _ExtendedGDBusPropertyInfo _device_property_info_local_address =
{
  {
    -1,
    (gchar *) "LocalAddress",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "local-address",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_local_name =
{
  {
    -1,
    (gchar *) "LocalName",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "local-name",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_power_state =
{
  {
    -1,
    (gchar *) "PowerState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "power-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_scan_state =
{
  {
    -1,
    (gchar *) "ScanState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "scan-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_pair_state =
{
  {
    -1,
    (gchar *) "PairState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "pair-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_bt_service_state =
{
  {
    -1,
    (gchar *) "BtServiceState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "bt-service-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_auto_connect_state =
{
  {
    -1,
    (gchar *) "AutoConnectState",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "auto-connect-state",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _device_property_info_testpoweron =
{
  {
    -1,
    (gchar *) "testpoweron",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "testpoweron",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _device_property_info_pointers[] =
{
  &_device_property_info_local_address,
  &_device_property_info_local_name,
  &_device_property_info_power_state,
  &_device_property_info_scan_state,
  &_device_property_info_pair_state,
  &_device_property_info_bt_service_state,
  &_device_property_info_auto_connect_state,
  &_device_property_info_testpoweron,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _device_interface_info =
{
  {
    -1,
    (gchar *) "com.hsaeyz.bluetooth.device",
    (GDBusMethodInfo **) &_device_method_info_pointers,
    (GDBusSignalInfo **) &_device_signal_info_pointers,
    (GDBusPropertyInfo **) &_device_property_info_pointers,
    NULL
  },
  "device",
};


/**
 * device_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
device_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_device_interface_info.parent_struct;
}

/**
 * device_override_properties:
 * @klass: The class structure for a #GObject<!-- -->-derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #Device interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
device_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "local-address");
  g_object_class_override_property (klass, property_id_begin++, "local-name");
  g_object_class_override_property (klass, property_id_begin++, "power-state");
  g_object_class_override_property (klass, property_id_begin++, "scan-state");
  g_object_class_override_property (klass, property_id_begin++, "pair-state");
  g_object_class_override_property (klass, property_id_begin++, "bt-service-state");
  g_object_class_override_property (klass, property_id_begin++, "auto-connect-state");
  g_object_class_override_property (klass, property_id_begin++, "testpoweron");
  return property_id_begin - 1;
}



/**
 * Device:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link>.
 */

/**
 * DeviceIface:
 * @parent_iface: The parent interface.
 * @handle_accpect_pair: Handler for the #Device::handle-accpect-pair signal.
 * @handle_clear_pair_device: Handler for the #Device::handle-clear-pair-device signal.
 * @handle_connect_device: Handler for the #Device::handle-connect-device signal.
 * @handle_delete_pair_device: Handler for the #Device::handle-delete-pair-device signal.
 * @handle_dis_connect_current_device: Handler for the #Device::handle-dis-connect-current-device signal.
 * @handle_dis_connect_device: Handler for the #Device::handle-dis-connect-device signal.
 * @handle_get_current_states: Handler for the #Device::handle-get-current-states signal.
 * @handle_get_local_properties: Handler for the #Device::handle-get-local-properties signal.
 * @handle_get_paired_list: Handler for the #Device::handle-get-paired-list signal.
 * @handle_refuse_pair: Handler for the #Device::handle-refuse-pair signal.
 * @handle_start_scan: Handler for the #Device::handle-start-scan signal.
 * @handle_stop_scan: Handler for the #Device::handle-stop-scan signal.
 * @get_auto_connect_state: Getter for the #Device:auto-connect-state property.
 * @get_bt_service_state: Getter for the #Device:bt-service-state property.
 * @get_local_address: Getter for the #Device:local-address property.
 * @get_local_name: Getter for the #Device:local-name property.
 * @get_pair_state: Getter for the #Device:pair-state property.
 * @get_power_state: Getter for the #Device:power-state property.
 * @get_scan_state: Getter for the #Device:scan-state property.
 * @get_testpoweron: Getter for the #Device:testpoweron property.
 * @aclconnectstate: Handler for the #Device::aclconnectstate signal.
 * @battery_changed: Handler for the #Device::battery-changed signal.
 * @connect_state_changed: Handler for the #Device::connect-state-changed signal.
 * @current_state_changed: Handler for the #Device::current-state-changed signal.
 * @disconnectreason: Handler for the #Device::disconnectreason signal.
 * @local_property_changed: Handler for the #Device::local-property-changed signal.
 * @pair_list_changed: Handler for the #Device::pair-list-changed signal.
 * @requestpair: Handler for the #Device::requestpair signal.
 * @search_device_changed: Handler for the #Device::search-device-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link>.
 */

typedef DeviceIface DeviceInterface;
G_DEFINE_INTERFACE (Device, device, G_TYPE_OBJECT);

static void
device_default_init (DeviceIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * Device::handle-start-scan:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.startScan">startScan()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_start_scan() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-start-scan",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_start_scan),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-stop-scan:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.stopScan">stopScan()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_stop_scan() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-scan",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_stop_scan),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-accpect-pair:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.accpectPair">accpectPair()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_accpect_pair() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-accpect-pair",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_accpect_pair),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-refuse-pair:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.refusePair">refusePair()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_refuse_pair() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-refuse-pair",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_refuse_pair),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-delete-pair-device:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_btAddress: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.deletePairDevice">deletePairDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_delete_pair_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-delete-pair-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_delete_pair_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * Device::handle-clear-pair-device:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.clearPairDevice">clearPairDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_clear_pair_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-clear-pair-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_clear_pair_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-connect-device:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_btAddress: Argument passed by remote caller.
   * @arg_profileId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.connectDevice">connectDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_connect_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-connect-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_connect_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Device::handle-dis-connect-device:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_btAddress: Argument passed by remote caller.
   * @arg_profileId: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectDevice">disConnectDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_dis_connect_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dis-connect-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_dis_connect_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Device::handle-dis-connect-current-device:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectCurrentDevice">disConnectCurrentDevice()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_dis_connect_current_device() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-dis-connect-current-device",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_dis_connect_current_device),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-get-paired-list:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getPairedList">getPairedList()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_get_paired_list() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-paired-list",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_get_paired_list),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-get-local-properties:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getLocalProperties">getLocalProperties()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_get_local_properties() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-local-properties",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_get_local_properties),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * Device::handle-get-current-states:
   * @object: A #Device.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getCurrentStates">getCurrentStates()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call device_complete_get_current_states() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-current-states",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, handle_get_current_states),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * Device::pair-list-changed:
   * @object: A #Device.
   * @arg_pairList: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.pairListChanged">"pairListChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("pair-list-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, pair_list_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_STRING);

  /**
   * Device::requestpair:
   * @object: A #Device.
   * @arg_phonename: Argument.
   * @arg_phoneaddr: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.requestpair">"requestpair"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("requestpair",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, requestpair),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * Device::aclconnectstate:
   * @object: A #Device.
   * @arg_mac: Argument.
   * @arg_state: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.aclconnectstate">"aclconnectstate"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("aclconnectstate",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, aclconnectstate),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Device::disconnectreason:
   * @object: A #Device.
   * @arg_mac: Argument.
   * @arg_reason: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.disconnectreason">"disconnectreason"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("disconnectreason",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, disconnectreason),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_INT);

  /**
   * Device::current-state-changed:
   * @object: A #Device.
   * @arg_btAddress: Argument.
   * @arg_hfpConnnectState: Argument.
   * @arg_pbapConnnectState: Argument.
   * @arg_avrcpConnnectState: Argument.
   * @arg_a2cpConnnectState: Argument.
   * @arg_callState: Argument.
   * @arg_phoneNumber: Argument.
   * @arg_audioState: Argument.
   * @arg_playState: Argument.
   * @arg_songLength: Argument.
   * @arg_playPosition: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.currentStateChanged">"currentStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("current-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, current_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    11, G_TYPE_STRING, G_TYPE_INT, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN, G_TYPE_BOOLEAN, G_TYPE_INT, G_TYPE_STRING, G_TYPE_BOOLEAN, G_TYPE_INT, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * Device::connect-state-changed:
   * @object: A #Device.
   * @arg_address: Argument.
   * @arg_profileId: Argument.
   * @arg_connectState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.connectStateChanged">"connectStateChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("connect-state-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, connect_state_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_STRING, G_TYPE_INT, G_TYPE_INT);

  /**
   * Device::local-property-changed:
   * @object: A #Device.
   * @arg_address: Argument.
   * @arg_localName: Argument.
   * @arg_powerState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.localPropertyChanged">"localPropertyChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("local-property-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, local_property_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    3, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_BOOLEAN);

  /**
   * Device::search-device-changed:
   * @object: A #Device.
   * @arg_address: Argument.
   * @arg_localName: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.searchDeviceChanged">"searchDeviceChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("search-device-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, search_device_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    2, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * Device::battery-changed:
   * @object: A #Device.
   * @arg_batteryState: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.batteryChanged">"batteryChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("battery-changed",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DeviceIface, battery_changed),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_INT);

  /* GObject properties for D-Bus properties: */
  /**
   * Device:local-address:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalAddress">"LocalAddress"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("local-address", "LocalAddress", "LocalAddress", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:local-name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalName">"LocalName"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("local-name", "LocalName", "LocalName", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:power-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PowerState">"PowerState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("power-state", "PowerState", "PowerState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:scan-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.ScanState">"ScanState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("scan-state", "ScanState", "ScanState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:pair-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PairState">"PairState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("pair-state", "PairState", "PairState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:bt-service-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.BtServiceState">"BtServiceState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("bt-service-state", "BtServiceState", "BtServiceState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:auto-connect-state:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.AutoConnectState">"AutoConnectState"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("auto-connect-state", "AutoConnectState", "AutoConnectState", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * Device:testpoweron:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.testpoweron">"testpoweron"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("testpoweron", "testpoweron", "testpoweron", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * device_get_local_address: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalAddress">"LocalAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use device_dup_local_address() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
device_get_local_address (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_local_address (object);
}

/**
 * device_dup_local_address: (skip)
 * @object: A #Device.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalAddress">"LocalAddress"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
device_dup_local_address (Device *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "local-address", &value, NULL);
  return value;
}

/**
 * device_set_local_address: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalAddress">"LocalAddress"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_local_address (Device *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "local-address", value, NULL);
}

/**
 * device_get_local_name: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use device_dup_local_name() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
device_get_local_name (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_local_name (object);
}

/**
 * device_dup_local_name: (skip)
 * @object: A #Device.
 *
 * Gets a copy of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalName">"LocalName"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
device_dup_local_name (Device *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "local-name", &value, NULL);
  return value;
}

/**
 * device_set_local_name: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.LocalName">"LocalName"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_local_name (Device *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "local-name", value, NULL);
}

/**
 * device_get_power_state: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PowerState">"PowerState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
device_get_power_state (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_power_state (object);
}

/**
 * device_set_power_state: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PowerState">"PowerState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_power_state (Device *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "power-state", value, NULL);
}

/**
 * device_get_scan_state: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.ScanState">"ScanState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
device_get_scan_state (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_scan_state (object);
}

/**
 * device_set_scan_state: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.ScanState">"ScanState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_scan_state (Device *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "scan-state", value, NULL);
}

/**
 * device_get_pair_state: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PairState">"PairState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
device_get_pair_state (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_pair_state (object);
}

/**
 * device_set_pair_state: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.PairState">"PairState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_pair_state (Device *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "pair-state", value, NULL);
}

/**
 * device_get_bt_service_state: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.BtServiceState">"BtServiceState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
device_get_bt_service_state (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_bt_service_state (object);
}

/**
 * device_set_bt_service_state: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.BtServiceState">"BtServiceState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_bt_service_state (Device *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "bt-service-state", value, NULL);
}

/**
 * device_get_auto_connect_state: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.AutoConnectState">"AutoConnectState"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
device_get_auto_connect_state (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_auto_connect_state (object);
}

/**
 * device_set_auto_connect_state: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.AutoConnectState">"AutoConnectState"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_auto_connect_state (Device *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "auto-connect-state", value, NULL);
}

/**
 * device_get_testpoweron: (skip)
 * @object: A #Device.
 *
 * Gets the value of the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.testpoweron">"testpoweron"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
device_get_testpoweron (Device *object)
{
  return DEVICE_GET_IFACE (object)->get_testpoweron (object);
}

/**
 * device_set_testpoweron: (skip)
 * @object: A #Device.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-com-hsaeyz-bluetooth-device.testpoweron">"testpoweron"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
device_set_testpoweron (Device *object, gint value)
{
  g_object_set (G_OBJECT (object), "testpoweron", value, NULL);
}

/**
 * device_emit_pair_list_changed:
 * @object: A #Device.
 * @arg_pairList: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.pairListChanged">"pairListChanged"</link> D-Bus signal.
 */
void
device_emit_pair_list_changed (
    Device *object,
    const gchar *arg_pairList)
{
  g_signal_emit_by_name (object, "pair-list-changed", arg_pairList);
}

/**
 * device_emit_requestpair:
 * @object: A #Device.
 * @arg_phonename: Argument to pass with the signal.
 * @arg_phoneaddr: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.requestpair">"requestpair"</link> D-Bus signal.
 */
void
device_emit_requestpair (
    Device *object,
    const gchar *arg_phonename,
    const gchar *arg_phoneaddr)
{
  g_signal_emit_by_name (object, "requestpair", arg_phonename, arg_phoneaddr);
}

/**
 * device_emit_aclconnectstate:
 * @object: A #Device.
 * @arg_mac: Argument to pass with the signal.
 * @arg_state: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.aclconnectstate">"aclconnectstate"</link> D-Bus signal.
 */
void
device_emit_aclconnectstate (
    Device *object,
    const gchar *arg_mac,
    gint arg_state)
{
  g_signal_emit_by_name (object, "aclconnectstate", arg_mac, arg_state);
}

/**
 * device_emit_disconnectreason:
 * @object: A #Device.
 * @arg_mac: Argument to pass with the signal.
 * @arg_reason: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.disconnectreason">"disconnectreason"</link> D-Bus signal.
 */
void
device_emit_disconnectreason (
    Device *object,
    const gchar *arg_mac,
    gint arg_reason)
{
  g_signal_emit_by_name (object, "disconnectreason", arg_mac, arg_reason);
}

/**
 * device_emit_current_state_changed:
 * @object: A #Device.
 * @arg_btAddress: Argument to pass with the signal.
 * @arg_hfpConnnectState: Argument to pass with the signal.
 * @arg_pbapConnnectState: Argument to pass with the signal.
 * @arg_avrcpConnnectState: Argument to pass with the signal.
 * @arg_a2cpConnnectState: Argument to pass with the signal.
 * @arg_callState: Argument to pass with the signal.
 * @arg_phoneNumber: Argument to pass with the signal.
 * @arg_audioState: Argument to pass with the signal.
 * @arg_playState: Argument to pass with the signal.
 * @arg_songLength: Argument to pass with the signal.
 * @arg_playPosition: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.currentStateChanged">"currentStateChanged"</link> D-Bus signal.
 */
void
device_emit_current_state_changed (
    Device *object,
    const gchar *arg_btAddress,
    gint arg_hfpConnnectState,
    gboolean arg_pbapConnnectState,
    gboolean arg_avrcpConnnectState,
    gboolean arg_a2cpConnnectState,
    gint arg_callState,
    const gchar *arg_phoneNumber,
    gboolean arg_audioState,
    gint arg_playState,
    const gchar *arg_songLength,
    const gchar *arg_playPosition)
{
  g_signal_emit_by_name (object, "current-state-changed", arg_btAddress, arg_hfpConnnectState, arg_pbapConnnectState, arg_avrcpConnnectState, arg_a2cpConnnectState, arg_callState, arg_phoneNumber, arg_audioState, arg_playState, arg_songLength, arg_playPosition);
}

/**
 * device_emit_connect_state_changed:
 * @object: A #Device.
 * @arg_address: Argument to pass with the signal.
 * @arg_profileId: Argument to pass with the signal.
 * @arg_connectState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.connectStateChanged">"connectStateChanged"</link> D-Bus signal.
 */
void
device_emit_connect_state_changed (
    Device *object,
    const gchar *arg_address,
    gint arg_profileId,
    gint arg_connectState)
{
  g_signal_emit_by_name (object, "connect-state-changed", arg_address, arg_profileId, arg_connectState);
}

/**
 * device_emit_local_property_changed:
 * @object: A #Device.
 * @arg_address: Argument to pass with the signal.
 * @arg_localName: Argument to pass with the signal.
 * @arg_powerState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.localPropertyChanged">"localPropertyChanged"</link> D-Bus signal.
 */
void
device_emit_local_property_changed (
    Device *object,
    const gchar *arg_address,
    const gchar *arg_localName,
    gboolean arg_powerState)
{
  g_signal_emit_by_name (object, "local-property-changed", arg_address, arg_localName, arg_powerState);
}

/**
 * device_emit_search_device_changed:
 * @object: A #Device.
 * @arg_address: Argument to pass with the signal.
 * @arg_localName: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.searchDeviceChanged">"searchDeviceChanged"</link> D-Bus signal.
 */
void
device_emit_search_device_changed (
    Device *object,
    const gchar *arg_address,
    const gchar *arg_localName)
{
  g_signal_emit_by_name (object, "search-device-changed", arg_address, arg_localName);
}

/**
 * device_emit_battery_changed:
 * @object: A #Device.
 * @arg_batteryState: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-com-hsaeyz-bluetooth-device.batteryChanged">"batteryChanged"</link> D-Bus signal.
 */
void
device_emit_battery_changed (
    Device *object,
    gint arg_batteryState)
{
  g_signal_emit_by_name (object, "battery-changed", arg_batteryState);
}

/**
 * device_call_start_scan:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.startScan">startScan()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_start_scan_finish() to get the result of the operation.
 *
 * See device_call_start_scan_sync() for the synchronous, blocking version of this method.
 */
void
device_call_start_scan (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "startScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_start_scan_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_start_scan().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_start_scan().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_start_scan_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_start_scan_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.startScan">startScan()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_start_scan() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_start_scan_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "startScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_stop_scan:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.stopScan">stopScan()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_stop_scan_finish() to get the result of the operation.
 *
 * See device_call_stop_scan_sync() for the synchronous, blocking version of this method.
 */
void
device_call_stop_scan (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "stopScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_stop_scan_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_stop_scan().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_stop_scan().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_stop_scan_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_stop_scan_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.stopScan">stopScan()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_stop_scan() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_stop_scan_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "stopScan",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_accpect_pair:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.accpectPair">accpectPair()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_accpect_pair_finish() to get the result of the operation.
 *
 * See device_call_accpect_pair_sync() for the synchronous, blocking version of this method.
 */
void
device_call_accpect_pair (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "accpectPair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_accpect_pair_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_accpect_pair().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_accpect_pair().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_accpect_pair_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_accpect_pair_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.accpectPair">accpectPair()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_accpect_pair() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_accpect_pair_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "accpectPair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_refuse_pair:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.refusePair">refusePair()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_refuse_pair_finish() to get the result of the operation.
 *
 * See device_call_refuse_pair_sync() for the synchronous, blocking version of this method.
 */
void
device_call_refuse_pair (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "refusePair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_refuse_pair_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_refuse_pair().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_refuse_pair().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_refuse_pair_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_refuse_pair_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.refusePair">refusePair()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_refuse_pair() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_refuse_pair_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "refusePair",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_delete_pair_device:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.deletePairDevice">deletePairDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_delete_pair_device_finish() to get the result of the operation.
 *
 * See device_call_delete_pair_device_sync() for the synchronous, blocking version of this method.
 */
void
device_call_delete_pair_device (
    Device *proxy,
    const gchar *arg_btAddress,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "deletePairDevice",
    g_variant_new ("(s)",
                   arg_btAddress),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_delete_pair_device_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_delete_pair_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_delete_pair_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_delete_pair_device_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_delete_pair_device_sync:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.deletePairDevice">deletePairDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_delete_pair_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_delete_pair_device_sync (
    Device *proxy,
    const gchar *arg_btAddress,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "deletePairDevice",
    g_variant_new ("(s)",
                   arg_btAddress),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_clear_pair_device:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.clearPairDevice">clearPairDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_clear_pair_device_finish() to get the result of the operation.
 *
 * See device_call_clear_pair_device_sync() for the synchronous, blocking version of this method.
 */
void
device_call_clear_pair_device (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "clearPairDevice",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_clear_pair_device_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_clear_pair_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_clear_pair_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_clear_pair_device_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_clear_pair_device_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.clearPairDevice">clearPairDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_clear_pair_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_clear_pair_device_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "clearPairDevice",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_connect_device:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @arg_profileId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.connectDevice">connectDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_connect_device_finish() to get the result of the operation.
 *
 * See device_call_connect_device_sync() for the synchronous, blocking version of this method.
 */
void
device_call_connect_device (
    Device *proxy,
    const gchar *arg_btAddress,
    gint arg_profileId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "connectDevice",
    g_variant_new ("(si)",
                   arg_btAddress,
                   arg_profileId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_connect_device_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_connect_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_connect_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_connect_device_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_connect_device_sync:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @arg_profileId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.connectDevice">connectDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_connect_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_connect_device_sync (
    Device *proxy,
    const gchar *arg_btAddress,
    gint arg_profileId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "connectDevice",
    g_variant_new ("(si)",
                   arg_btAddress,
                   arg_profileId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_dis_connect_device:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @arg_profileId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectDevice">disConnectDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_dis_connect_device_finish() to get the result of the operation.
 *
 * See device_call_dis_connect_device_sync() for the synchronous, blocking version of this method.
 */
void
device_call_dis_connect_device (
    Device *proxy,
    const gchar *arg_btAddress,
    gint arg_profileId,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "disConnectDevice",
    g_variant_new ("(si)",
                   arg_btAddress,
                   arg_profileId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_dis_connect_device_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_dis_connect_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_dis_connect_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_dis_connect_device_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_dis_connect_device_sync:
 * @proxy: A #DeviceProxy.
 * @arg_btAddress: Argument to pass with the method invocation.
 * @arg_profileId: Argument to pass with the method invocation.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectDevice">disConnectDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_dis_connect_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_dis_connect_device_sync (
    Device *proxy,
    const gchar *arg_btAddress,
    gint arg_profileId,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "disConnectDevice",
    g_variant_new ("(si)",
                   arg_btAddress,
                   arg_profileId),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_dis_connect_current_device:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectCurrentDevice">disConnectCurrentDevice()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_dis_connect_current_device_finish() to get the result of the operation.
 *
 * See device_call_dis_connect_current_device_sync() for the synchronous, blocking version of this method.
 */
void
device_call_dis_connect_current_device (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "disConnectCurrentDevice",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_dis_connect_current_device_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_dis_connect_current_device().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_dis_connect_current_device().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_dis_connect_current_device_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_dis_connect_current_device_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectCurrentDevice">disConnectCurrentDevice()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_dis_connect_current_device() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_dis_connect_current_device_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "disConnectCurrentDevice",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_paired_list:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getPairedList">getPairedList()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_get_paired_list_finish() to get the result of the operation.
 *
 * See device_call_get_paired_list_sync() for the synchronous, blocking version of this method.
 */
void
device_call_get_paired_list (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getPairedList",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_get_paired_list_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_get_paired_list().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_get_paired_list().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_paired_list_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_paired_list_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getPairedList">getPairedList()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_get_paired_list() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_paired_list_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getPairedList",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_local_properties:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getLocalProperties">getLocalProperties()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_get_local_properties_finish() to get the result of the operation.
 *
 * See device_call_get_local_properties_sync() for the synchronous, blocking version of this method.
 */
void
device_call_get_local_properties (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getLocalProperties",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_get_local_properties_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_get_local_properties().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_get_local_properties().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_local_properties_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_local_properties_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getLocalProperties">getLocalProperties()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_get_local_properties() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_local_properties_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getLocalProperties",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_current_states:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getCurrentStates">getCurrentStates()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_call_get_current_states_finish() to get the result of the operation.
 *
 * See device_call_get_current_states_sync() for the synchronous, blocking version of this method.
 */
void
device_call_get_current_states (
    Device *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "getCurrentStates",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * device_call_get_current_states_finish:
 * @proxy: A #DeviceProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_call_get_current_states().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with device_call_get_current_states().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_current_states_finish (
    Device *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_call_get_current_states_sync:
 * @proxy: A #DeviceProxy.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getCurrentStates">getCurrentStates()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See device_call_get_current_states() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
device_call_get_current_states_sync (
    Device *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "getCurrentStates",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * device_complete_start_scan:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.startScan">startScan()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_start_scan (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_stop_scan:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.stopScan">stopScan()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_stop_scan (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_accpect_pair:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.accpectPair">accpectPair()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_accpect_pair (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_refuse_pair:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.refusePair">refusePair()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_refuse_pair (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_delete_pair_device:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.deletePairDevice">deletePairDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_delete_pair_device (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_clear_pair_device:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.clearPairDevice">clearPairDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_clear_pair_device (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_connect_device:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.connectDevice">connectDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_connect_device (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_dis_connect_device:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectDevice">disConnectDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_dis_connect_device (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_dis_connect_current_device:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.disConnectCurrentDevice">disConnectCurrentDevice()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_dis_connect_current_device (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_get_paired_list:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getPairedList">getPairedList()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_get_paired_list (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_get_local_properties:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getLocalProperties">getLocalProperties()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_get_local_properties (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * device_complete_get_current_states:
 * @object: A #Device.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-com-hsaeyz-bluetooth-device.getCurrentStates">getCurrentStates()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
device_complete_get_current_states (
    Device *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * DeviceProxy:
 *
 * The #DeviceProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DeviceProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DeviceProxy.
 */

struct _DeviceProxyPrivate
{
  GData *qdata;
};

static void device_proxy_iface_init (DeviceIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DeviceProxy, device_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DeviceProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_DEVICE, device_proxy_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (DeviceProxy, device_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_DEVICE, device_proxy_iface_init));

#endif
static void
device_proxy_finalize (GObject *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (device_proxy_parent_class)->finalize (object);
}

static void
device_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = _device_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
device_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface com.hsaeyz.bluetooth.device: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
device_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = _device_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "com.hsaeyz.bluetooth.device", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) device_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
device_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_device_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_DEVICE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_DEVICE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
device_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DeviceProxy *proxy = DEVICE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_device_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_device_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
device_proxy_get_local_address (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LocalAddress");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
device_proxy_get_local_name (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LocalName");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
device_proxy_get_power_state (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PowerState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
device_proxy_get_scan_state (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "ScanState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
device_proxy_get_pair_state (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PairState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
device_proxy_get_bt_service_state (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "BtServiceState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
device_proxy_get_auto_connect_state (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "AutoConnectState");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
device_proxy_get_testpoweron (Device *object)
{
  DeviceProxy *proxy = DEVICE_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "testpoweron");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
device_proxy_init (DeviceProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = device_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_DEVICE_PROXY, DeviceProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), device_interface_info ());
}

static void
device_proxy_class_init (DeviceProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = device_proxy_finalize;
  gobject_class->get_property = device_proxy_get_property;
  gobject_class->set_property = device_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = device_proxy_g_signal;
  proxy_class->g_properties_changed = device_proxy_g_properties_changed;

  device_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DeviceProxyPrivate));
#endif
}

static void
device_proxy_iface_init (DeviceIface *iface)
{
  iface->get_local_address = device_proxy_get_local_address;
  iface->get_local_name = device_proxy_get_local_name;
  iface->get_power_state = device_proxy_get_power_state;
  iface->get_scan_state = device_proxy_get_scan_state;
  iface->get_pair_state = device_proxy_get_pair_state;
  iface->get_bt_service_state = device_proxy_get_bt_service_state;
  iface->get_auto_connect_state = device_proxy_get_auto_connect_state;
  iface->get_testpoweron = device_proxy_get_testpoweron;
}

/**
 * device_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_proxy_new_finish() to get the result of the operation.
 *
 * See device_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
device_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_DEVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.device", NULL);
}

/**
 * device_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with device_proxy_new().
 *
 * Returns: (transfer full) (type DeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
Device *
device_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DEVICE (ret);
  else
    return NULL;
}

/**
 * device_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (allow-none): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See device_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
Device *
device_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_DEVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.device", NULL);
  if (ret != NULL)
    return DEVICE (ret);
  else
    return NULL;
}


/**
 * device_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like device_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call device_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See device_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
device_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_DEVICE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.device", NULL);
}

/**
 * device_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to device_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with device_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
Device *
device_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DEVICE (ret);
  else
    return NULL;
}

/**
 * device_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (allow-none): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like device_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See device_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DeviceProxy): The constructed proxy object or %NULL if @error is set.
 */
Device *
device_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_DEVICE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "com.hsaeyz.bluetooth.device", NULL);
  if (ret != NULL)
    return DEVICE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DeviceSkeleton:
 *
 * The #DeviceSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DeviceSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DeviceSkeleton.
 */

struct _DeviceSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_device_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  guint num_params;
  guint num_extra;
  guint n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_DEVICE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_DEVICE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_device_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_device_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_device_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_device_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _device_skeleton_vtable =
{
  _device_skeleton_handle_method_call,
  _device_skeleton_handle_get_property,
  _device_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
device_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return device_interface_info ();
}

static GDBusInterfaceVTable *
device_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_device_skeleton_vtable;
}

static GVariant *
device_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_device_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _device_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _device_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _device_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _device_emit_changed (gpointer user_data);

static void
device_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _device_emit_changed (skeleton);
}

static void
_device_on_signal_pair_list_changed (
    Device *object,
    const gchar *arg_pairList)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s)",
                   arg_pairList));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "pairListChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_requestpair (
    Device *object,
    const gchar *arg_phonename,
    const gchar *arg_phoneaddr)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ss)",
                   arg_phonename,
                   arg_phoneaddr));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "requestpair",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_aclconnectstate (
    Device *object,
    const gchar *arg_mac,
    gint arg_state)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(si)",
                   arg_mac,
                   arg_state));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "aclconnectstate",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_disconnectreason (
    Device *object,
    const gchar *arg_mac,
    gint arg_reason)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(si)",
                   arg_mac,
                   arg_reason));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "disconnectreason",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_current_state_changed (
    Device *object,
    const gchar *arg_btAddress,
    gint arg_hfpConnnectState,
    gboolean arg_pbapConnnectState,
    gboolean arg_avrcpConnnectState,
    gboolean arg_a2cpConnnectState,
    gint arg_callState,
    const gchar *arg_phoneNumber,
    gboolean arg_audioState,
    gint arg_playState,
    const gchar *arg_songLength,
    const gchar *arg_playPosition)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(sibbbisbiss)",
                   arg_btAddress,
                   arg_hfpConnnectState,
                   arg_pbapConnnectState,
                   arg_avrcpConnnectState,
                   arg_a2cpConnnectState,
                   arg_callState,
                   arg_phoneNumber,
                   arg_audioState,
                   arg_playState,
                   arg_songLength,
                   arg_playPosition));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "currentStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_connect_state_changed (
    Device *object,
    const gchar *arg_address,
    gint arg_profileId,
    gint arg_connectState)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(sii)",
                   arg_address,
                   arg_profileId,
                   arg_connectState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "connectStateChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_local_property_changed (
    Device *object,
    const gchar *arg_address,
    const gchar *arg_localName,
    gboolean arg_powerState)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ssb)",
                   arg_address,
                   arg_localName,
                   arg_powerState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "localPropertyChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_search_device_changed (
    Device *object,
    const gchar *arg_address,
    const gchar *arg_localName)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(ss)",
                   arg_address,
                   arg_localName));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "searchDeviceChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void
_device_on_signal_battery_changed (
    Device *object,
    gint arg_batteryState)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(i)",
                   arg_batteryState));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "com.hsaeyz.bluetooth.device", "batteryChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void device_skeleton_iface_init (DeviceIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DeviceSkeleton, device_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DeviceSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_DEVICE, device_skeleton_iface_init));

#else
G_DEFINE_TYPE_WITH_CODE (DeviceSkeleton, device_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_DEVICE, device_skeleton_iface_init));

#endif
static void
device_skeleton_finalize (GObject *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  guint n;
  for (n = 0; n < 8; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (device_skeleton_parent_class)->finalize (object);
}

static void
device_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_device_emit_changed (gpointer user_data)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "com.hsaeyz.bluetooth.device",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_device_schedule_emit_changed (DeviceSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
device_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _device_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
device_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _device_schedule_emit_changed (skeleton, _device_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
device_skeleton_init (DeviceSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = device_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_DEVICE_SKELETON, DeviceSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 8);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_INT);
}

static const gchar *
device_skeleton_get_local_address (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
device_skeleton_get_local_name (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
device_skeleton_get_power_state (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
device_skeleton_get_scan_state (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
device_skeleton_get_pair_state (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
device_skeleton_get_bt_service_state (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
device_skeleton_get_auto_connect_state (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
device_skeleton_get_testpoweron (Device *object)
{
  DeviceSkeleton *skeleton = DEVICE_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
device_skeleton_class_init (DeviceSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = device_skeleton_finalize;
  gobject_class->get_property = device_skeleton_get_property;
  gobject_class->set_property = device_skeleton_set_property;
  gobject_class->notify       = device_skeleton_notify;


  device_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = device_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = device_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = device_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = device_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DeviceSkeletonPrivate));
#endif
}

static void
device_skeleton_iface_init (DeviceIface *iface)
{
  iface->pair_list_changed = _device_on_signal_pair_list_changed;
  iface->requestpair = _device_on_signal_requestpair;
  iface->aclconnectstate = _device_on_signal_aclconnectstate;
  iface->disconnectreason = _device_on_signal_disconnectreason;
  iface->current_state_changed = _device_on_signal_current_state_changed;
  iface->connect_state_changed = _device_on_signal_connect_state_changed;
  iface->local_property_changed = _device_on_signal_local_property_changed;
  iface->search_device_changed = _device_on_signal_search_device_changed;
  iface->battery_changed = _device_on_signal_battery_changed;
  iface->get_local_address = device_skeleton_get_local_address;
  iface->get_local_name = device_skeleton_get_local_name;
  iface->get_power_state = device_skeleton_get_power_state;
  iface->get_scan_state = device_skeleton_get_scan_state;
  iface->get_pair_state = device_skeleton_get_pair_state;
  iface->get_bt_service_state = device_skeleton_get_bt_service_state;
  iface->get_auto_connect_state = device_skeleton_get_auto_connect_state;
  iface->get_testpoweron = device_skeleton_get_testpoweron;
}

/**
 * device_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-com-hsaeyz-bluetooth-device.top_of_page">com.hsaeyz.bluetooth.device</link>.
 *
 * Returns: (transfer full) (type DeviceSkeleton): The skeleton object.
 */
Device *
device_skeleton_new (void)
{
  return DEVICE (g_object_new (TYPE_DEVICE_SKELETON, NULL));
}

